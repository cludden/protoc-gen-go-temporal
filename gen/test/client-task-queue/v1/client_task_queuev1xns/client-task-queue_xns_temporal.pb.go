// Code generated by protoc-gen-go_temporal. DO NOT EDIT.
// versions:
//
//	protoc-gen-go_temporal dev (latest)
//	protoc (unknown)
//
// source: test/client-task-queue/v1/client-task-queue.proto
package client_task_queuev1xns

import (
	"context"
	"errors"
	"fmt"
	temporalv1 "github.com/cludden/protoc-gen-go-temporal/gen/temporal/v1"
	xnsv1 "github.com/cludden/protoc-gen-go-temporal/gen/temporal/xns/v1"
	v1 "github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1"
	convert "github.com/cludden/protoc-gen-go-temporal/pkg/convert"
	xns "github.com/cludden/protoc-gen-go-temporal/pkg/xns"
	uuid "github.com/google/uuid"
	enumsv1 "go.temporal.io/api/enums/v1"
	activity "go.temporal.io/sdk/activity"
	client "go.temporal.io/sdk/client"
	temporal "go.temporal.io/sdk/temporal"
	worker "go.temporal.io/sdk/worker"
	workflow "go.temporal.io/sdk/workflow"
	anypb "google.golang.org/protobuf/types/known/anypb"
	durationpb "google.golang.org/protobuf/types/known/durationpb"
	"time"
)

// ClientTaskQueueServiceOptions is used to configure test.client_task_queue.v1.ClientTaskQueueService xns activity registration
type ClientTaskQueueServiceOptions struct {
	// errorConverter is used to customize error
	errorConverter func(error) error
	// filter is used to filter xns activity registrations. It receives as
	// input the original activity name, and should return one of the following:
	// 1. the original activity name, for no changes
	// 2. a modified activity name, to override the original activity name
	// 3. an empty string, to skip registration
	filter func(string) string
}

// NewClientTaskQueueServiceOptions initializes a new ClientTaskQueueServiceOptions value
func NewClientTaskQueueServiceOptions() *ClientTaskQueueServiceOptions {
	return &ClientTaskQueueServiceOptions{}
}

// WithErrorConverter overrides the default error converter applied to xns activity errors
func (opts *ClientTaskQueueServiceOptions) WithErrorConverter(errorConverter func(error) error) *ClientTaskQueueServiceOptions {
	opts.errorConverter = errorConverter
	return opts
}

// Filter is used to filter registered xns activities or customize their name
func (opts *ClientTaskQueueServiceOptions) WithFilter(filter func(string) string) *ClientTaskQueueServiceOptions {
	opts.filter = filter
	return opts
}

// convertError is applied to all xns activity errors
func (opts *ClientTaskQueueServiceOptions) convertError(err error) error {
	if err == nil {
		return nil
	}
	if opts != nil && opts.errorConverter != nil {
		return opts.errorConverter(err)
	}
	return xns.ErrorToApplicationError(err)
}

// filterActivity is used to filter xns activity registrations
func (opts *ClientTaskQueueServiceOptions) filterActivity(name string) string {
	if opts == nil || opts.filter == nil {
		return name
	}
	return opts.filter(name)
}

// clientTaskQueueServiceOptions is a reference to the ClientTaskQueueServiceOptions initialized at registration
var clientTaskQueueServiceOptions *ClientTaskQueueServiceOptions

// RegisterClientTaskQueueServiceActivities registers test.client_task_queue.v1.ClientTaskQueueService cross-namespace activities
func RegisterClientTaskQueueServiceActivities(r worker.ActivityRegistry, c v1.ClientTaskQueueServiceClient, options ...*ClientTaskQueueServiceOptions) {
	if clientTaskQueueServiceOptions == nil && len(options) > 0 && options[0] != nil {
		clientTaskQueueServiceOptions = options[0]
	}
	a := &clientTaskQueueServiceActivities{c}
	if name := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.CancelWorkflow"); name != "" {
		r.RegisterActivityWithOptions(a.CancelWorkflow, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity(v1.FooWorkflowName); name != "" {
		r.RegisterActivityWithOptions(a.Foo, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.GetFoo"); name != "" {
		r.RegisterActivityWithOptions(a.GetFoo, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.FooWithBar"); name != "" {
		r.RegisterActivityWithOptions(a.FooWithBar, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.FooWithBaz"); name != "" {
		r.RegisterActivityWithOptions(a.FooWithBaz, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity(v1.QuxWorkflowName); name != "" {
		r.RegisterActivityWithOptions(a.Qux, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.GetQux"); name != "" {
		r.RegisterActivityWithOptions(a.GetQux, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.QuxWithBar"); name != "" {
		r.RegisterActivityWithOptions(a.QuxWithBar, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.QuxWithBaz"); name != "" {
		r.RegisterActivityWithOptions(a.QuxWithBaz, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity(v1.BarSignalName); name != "" {
		r.RegisterActivityWithOptions(a.Bar, activity.RegisterOptions{Name: name})
	}
	if name := clientTaskQueueServiceOptions.filterActivity(v1.BazUpdateName); name != "" {
		r.RegisterActivityWithOptions(a.Baz, activity.RegisterOptions{Name: name})
	}
}

// FooWorkflowOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow execution
type FooWorkflowOptions struct {
	ActivityOptions      *workflow.ActivityOptions
	Detached             bool
	HeartbeatInterval    time.Duration
	HeartbeatTimeout     time.Duration
	ParentClosePolicy    enumsv1.ParentClosePolicy
	StartWorkflowOptions *client.StartWorkflowOptions
}

// NewFooWorkflowOptions initializes a new FooWorkflowOptions value
func NewFooWorkflowOptions() *FooWorkflowOptions {
	return &FooWorkflowOptions{}
}

// Build initializes the activity context and input
func (opts *FooWorkflowOptions) Build(ctx workflow.Context, input *v1.FooInput) (workflow.Context, *xnsv1.WorkflowRequest, error) {
	// initialize start workflow options
	swo := client.StartWorkflowOptions{}
	if opts.StartWorkflowOptions != nil {
		swo = *opts.StartWorkflowOptions
	}

	// initialize workflow id if not set
	if swo.ID == "" {
		if err := workflow.SideEffect(ctx, func(ctx workflow.Context) any {
			id, err := uuid.NewRandom()
			if err != nil {
				workflow.GetLogger(ctx).Error("error generating workflow id", "error", err)
				return nil
			}
			return id
		}).Get(&swo.ID); err != nil {
			return nil, nil, err
		}
	}
	if swo.ID == "" {
		return nil, nil, temporal.NewNonRetryableApplicationError("workflow id is required", "InvalidArgument", nil)
	}

	// marshal workflow request protobuf message
	inputpb, err := anypb.New(input)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling workflow request: %w", err)
	}

	// marshal start workflow options protobuf message
	swopb, err := xns.MarshalStartWorkflowOptions(swo)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling start workflow options: %w", err)
	}

	// marshal parent close policy protobuf message
	var parentClosePolicy temporalv1.ParentClosePolicy
	switch opts.ParentClosePolicy {
	case enumsv1.PARENT_CLOSE_POLICY_ABANDON:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_ABANDON
	case enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL
	case enumsv1.PARENT_CLOSE_POLICY_TERMINATE:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE
	}

	// initialize xns activity options
	ao := workflow.ActivityOptions{}
	if opts.ActivityOptions != nil {
		ao = *opts.ActivityOptions
	}

	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = time.Second * 60
	}

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = time.Hour * 24
	}

	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	// configure heartbeat interval
	if opts.HeartbeatInterval == 0 {
		opts.HeartbeatInterval = ao.HeartbeatTimeout / 2
	}

	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.WorkflowRequest{
		Detached:             opts.Detached,
		HeartbeatInterval:    durationpb.New(opts.HeartbeatInterval),
		ParentClosePolicy:    parentClosePolicy,
		Request:              inputpb,
		StartWorkflowOptions: swopb,
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (opts *FooWorkflowOptions) WithActivityOptions(ao workflow.ActivityOptions) *FooWorkflowOptions {
	opts.ActivityOptions = &ao
	return opts
}

// WithDetached can be used to start a workflow execution and exit immediately
func (opts *FooWorkflowOptions) WithDetached(d bool) *FooWorkflowOptions {
	opts.Detached = d
	return opts
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (opts *FooWorkflowOptions) WithHeartbeatInterval(d time.Duration) *FooWorkflowOptions {
	opts.HeartbeatInterval = d
	return opts
}

// WithHeartbeatTimeout can be used to customize the activity heartbeat timeout
func (opts *FooWorkflowOptions) WithHeartbeatTimeout(d time.Duration) *FooWorkflowOptions {
	opts.HeartbeatTimeout = d
	return opts
}

// WithParentClosePolicy can be used to customize the cancellation propagation behavior
func (opts *FooWorkflowOptions) WithParentClosePolicy(policy enumsv1.ParentClosePolicy) *FooWorkflowOptions {
	opts.ParentClosePolicy = policy
	return opts
}

// WithStartWorkflowOptions can be used to customize the start workflow options
func (opts *FooWorkflowOptions) WithStartWorkflow(swo client.StartWorkflowOptions) *FooWorkflowOptions {
	opts.StartWorkflowOptions = &swo
	return opts
}

// FooRun provides a handle to a test.client_task_queue.v1.ClientTaskQueueService.Foo workflow execution
type FooRun interface {
	// Cancel cancels the workflow
	Cancel(workflow.Context) error

	// Future returns the inner workflow.Future
	Future() workflow.Future

	// Get returns the inner workflow.Future
	Get(workflow.Context) (*v1.FooOutput, error)

	// ID returns the workflow id
	ID() string

	// Bar executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal and blocks until completion
	Bar(workflow.Context, *v1.BarInput, ...*BarSignalOptions) error

	// BarAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal and returns a handle to the underlying activity
	BarAsync(workflow.Context, *v1.BarInput, ...*BarSignalOptions) (BarSignalHandle, error)

	// Baz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and blocks until completion
	Baz(workflow.Context, *v1.BazInput, ...*BazUpdateOptions) (*v1.BazOutput, error)

	// BazAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and returns a handle to the underlying activity
	BazAsync(workflow.Context, *v1.BazInput, ...*BazUpdateOptions) (BazHandle, error)
}

// fooRun provides a(n) FooRun implementation
type fooRun struct {
	cancel            func()
	ctx               workflow.Context
	future            workflow.Future
	id                string
	heartbeatInterval time.Duration
	parentClosePolicy enumsv1.ParentClosePolicy
}

// Cancel the underlying workflow execution
func (r *fooRun) Cancel(ctx workflow.Context) error {
	if r.cancel != nil {
		r.cancel()
		if _, err := r.Get(ctx); err != nil && !errors.Is(err, workflow.ErrCanceled) {
			return err
		}
		return nil
	}
	return CancelClientTaskQueueServiceWorkflow(ctx, r.id, "")
}

// Future returns the underlying activity future
func (r *fooRun) Future() workflow.Future {
	if r.future == nil {
		rr := GetFooAsync(r.ctx, r.id, "").(*fooRun)
		r.future = rr.future
		r.cancel = rr.cancel
	}
	return r.future
}

// Get blocks on activity completion and returns the underlying workflow result
func (r *fooRun) Get(ctx workflow.Context) (*v1.FooOutput, error) {
	ctx, cancel := workflow.WithCancel(ctx)
	if r.future == nil {
		rr := GetFooAsync(ctx, r.id, "", NewGetFooOptions().WithParentClosePolicy(r.parentClosePolicy).WithHeartbeatInterval(r.heartbeatInterval)).(*fooRun)
		r.future = rr.future
		r.cancel = cancel
	}
	var resp v1.FooOutput
	if err := r.future.Get(ctx, &resp); err != nil {
		return nil, err
	}
	return &resp, nil
}

// ID returns the underlying workflow id
func (r *fooRun) ID() string {
	return r.id
}

// Bar executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal and blocks until the underlying activity completes
func (r *fooRun) Bar(ctx workflow.Context, req *v1.BarInput, opts ...*BarSignalOptions) error {
	return Bar(ctx, r.ID(), "", req, opts...)
}

// BarAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal and returns a handle to the underlying activity
func (r *fooRun) BarAsync(ctx workflow.Context, req *v1.BarInput, opts ...*BarSignalOptions) (BarSignalHandle, error) {
	return BarAsync(ctx, r.ID(), "", req, opts...)
}

// Baz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and blocks until completion
func (r *fooRun) Baz(ctx workflow.Context, req *v1.BazInput, opts ...*BazUpdateOptions) (*v1.BazOutput, error) {
	return Baz(ctx, r.ID(), "", req, opts...)
}

// BazAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and returns a handle to the underlying activity
func (r *fooRun) BazAsync(ctx workflow.Context, req *v1.BazInput, opts ...*BazUpdateOptions) (BazHandle, error) {
	return BazAsync(ctx, r.ID(), "", req, opts...)
}

// Foo executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow and blocks until error or response is received
func Foo(ctx workflow.Context, req *v1.FooInput, opts ...*FooWorkflowOptions) (*v1.FooOutput, error) {
	run, err := FooAsync(ctx, req, opts...)
	if err != nil {
		return nil, err
	}
	return run.Get(ctx)
}

// FooAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow and returns a handle to the underlying activity
func FooAsync(ctx workflow.Context, input *v1.FooInput, opts ...*FooWorkflowOptions) (FooRun, error) {
	activityName := clientTaskQueueServiceOptions.filterActivity(v1.FooWorkflowName)
	if activityName == "" {
		return nil, temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("no activity registered for %s", v1.FooWorkflowName),
			"Unimplemented",
			nil,
		)
	}

	var opt *FooWorkflowOptions
	if len(opts) > 0 && opts[0] != nil {
		opt = opts[0]
	} else {
		opt = NewFooWorkflowOptions()
	}
	ctx, req, err := opt.Build(ctx, input)
	if err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(err)
	}
	ctx, cancel := workflow.WithCancel(ctx)
	return &fooRun{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     req.GetStartWorkflowOptions().GetId(),
	}, nil
}

// GetFoo returns a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow execution
func GetFoo(ctx workflow.Context, workflowID string, runID string, options ...*GetFooOptions) (out *v1.FooOutput, err error) {
	out, err = GetFooAsync(ctx, workflowID, runID, options...).Get(ctx)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// GetFooAsync returns a handle to a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow execution
func GetFooAsync(ctx workflow.Context, workflowID string, runID string, options ...*GetFooOptions) FooRun {
	activityName := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.GetFoo")
	if activityName == "" {
		f, set := workflow.NewFuture(ctx)
		set.SetError(temporal.NewNonRetryableApplicationError(fmt.Sprintf("no activity registered for %s", activityName), "Unimplemented", nil))
		return &fooRun{
			future: f,
			id:     workflowID,
		}
	}
	var opt *GetFooOptions
	if len(options) > 0 && options[0] != nil {
		opt = options[0]
	} else {
		opt = NewGetFooOptions()
	}
	ctx, req, err := opt.Build(ctx, workflowID, runID)
	if err != nil {
		f, set := workflow.NewFuture(ctx)
		set.SetError(clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(fmt.Sprintf("no activity registered for %s", activityName), "Unimplemented", nil)))
		return &fooRun{
			future: f,
			id:     workflowID,
		}
	}
	ctx, cancel := workflow.WithCancel(ctx)
	return &fooRun{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     workflowID,
	}
}

// GetFooOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow execution getter activity
type GetFooOptions struct {
	activityOptions   *workflow.ActivityOptions
	heartbeatInterval time.Duration
	parentClosePolicy enumsv1.ParentClosePolicy
}

// NewGetFooOptions initializes a new GetFooOptions value
func NewGetFooOptions() *GetFooOptions {
	return &GetFooOptions{}
}

// Build initializes the activity context and input
func (opt *GetFooOptions) Build(ctx workflow.Context, workflowID string, runID string) (workflow.Context, *xnsv1.GetWorkflowRequest, error) {
	if opt.heartbeatInterval == 0 {
		opt.heartbeatInterval = 30000000000 // 30 seconds
	}

	// configure activity options
	var ao workflow.ActivityOptions
	if opt.activityOptions != nil {
		ao = *opt.activityOptions
	} else {
		ao = workflow.ActivityOptions{}
	}
	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = 60000000000 // 1 minute
	}
	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = 86400000000000 // 1 day
	}
	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.GetWorkflowRequest{
		HeartbeatInterval: durationpb.New(opt.heartbeatInterval),
		ParentClosePolicy: opt.parentClosePolicy,
		RunId:             runID,
		WorkflowId:        workflowID,
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (o *GetFooOptions) WithActivityOptions(ao workflow.ActivityOptions) *GetFooOptions {
	o.activityOptions = &ao
	return o
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (o *GetFooOptions) WithHeartbeatInterval(d time.Duration) *GetFooOptions {
	o.heartbeatInterval = d
	return o
}

// WithParentClosePolicy can be used to customize the cancellation propagation behavior
func (o *GetFooOptions) WithParentClosePolicy(policy enumsv1.ParentClosePolicy) *GetFooOptions {
	o.parentClosePolicy = policy
	return o
}

// FooWithBarOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.FooWithBar activity
type FooWithBarOptions struct {
	ActivityOptions      *workflow.ActivityOptions
	Detached             bool
	HeartbeatInterval    time.Duration
	HeartbeatTimeout     time.Duration
	ParentClosePolicy    enumsv1.ParentClosePolicy
	StartWorkflowOptions *client.StartWorkflowOptions
}

// NewFooWithBarOptions initializes a new FooWithBarOptions value
func NewFooWithBarOptions() *FooWithBarOptions {
	return &FooWithBarOptions{}
}

// Build initializes the activity context and input
func (opts *FooWithBarOptions) Build(ctx workflow.Context, input *v1.FooInput, signal *v1.BarInput) (workflow.Context, *xnsv1.WorkflowRequest, error) {
	// initialize start workflow options
	swo := client.StartWorkflowOptions{}
	if opts.StartWorkflowOptions != nil {
		swo = *opts.StartWorkflowOptions
	}

	// initialize workflow id if not set
	if swo.ID == "" {
		if err := workflow.SideEffect(ctx, func(ctx workflow.Context) any {
			id, err := uuid.NewRandom()
			if err != nil {
				workflow.GetLogger(ctx).Error("error generating workflow id", "error", err)
				return nil
			}
			return id
		}).Get(&swo.ID); err != nil {
			return nil, nil, err
		}
	}
	if swo.ID == "" {
		return nil, nil, temporal.NewNonRetryableApplicationError("workflow id is required", "InvalidArgument", nil)
	}

	// marshal workflow request protobuf message
	inputpb, err := anypb.New(input)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling workflow request: %w", err)
	}

	// marshal signal request protobuf message
	signalpb, err := anypb.New(signal)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling signal request: %w", err)
	}

	// marshal start workflow options protobuf message
	swopb, err := xns.MarshalStartWorkflowOptions(swo)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling start workflow options: %w", err)
	}

	// marshal parent close policy protobuf message
	var parentClosePolicy temporalv1.ParentClosePolicy
	switch opts.ParentClosePolicy {
	case enumsv1.PARENT_CLOSE_POLICY_ABANDON:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_ABANDON
	case enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL
	case enumsv1.PARENT_CLOSE_POLICY_TERMINATE:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE
	}

	// initialize xns activity options
	ao := workflow.ActivityOptions{}
	if opts.ActivityOptions != nil {
		ao = *opts.ActivityOptions
	}

	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = time.Second * 60
	}

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = time.Hour * 24
	}

	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	// configure heartbeat interval
	if opts.HeartbeatInterval == 0 {
		opts.HeartbeatInterval = ao.HeartbeatTimeout / 2
	}

	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.WorkflowRequest{
		Detached:             opts.Detached,
		HeartbeatInterval:    durationpb.New(opts.HeartbeatInterval),
		ParentClosePolicy:    parentClosePolicy,
		Request:              inputpb,
		Signal:               signalpb,
		StartWorkflowOptions: swopb,
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (opts *FooWithBarOptions) WithActivityOptions(ao workflow.ActivityOptions) *FooWithBarOptions {
	opts.ActivityOptions = &ao
	return opts
}

// WithDetached can be used to start a workflow execution and exit immediately
func (opts *FooWithBarOptions) WithDetached(d bool) *FooWithBarOptions {
	opts.Detached = d
	return opts
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (opts *FooWithBarOptions) WithHeartbeatInterval(d time.Duration) *FooWithBarOptions {
	opts.HeartbeatInterval = d
	return opts
}

// WithHeartbeatTimeout can be used to customize the activity heartbeat timeout
func (opts *FooWithBarOptions) WithHeartbeatTimeout(d time.Duration) *FooWithBarOptions {
	opts.HeartbeatTimeout = d
	return opts
}

// WithParentClosePolicy can be used to customize the cancellation propagation behavior
func (opts *FooWithBarOptions) WithParentClosePolicy(policy enumsv1.ParentClosePolicy) *FooWithBarOptions {
	opts.ParentClosePolicy = policy
	return opts
}

// WithStartWorkflowOptions can be used to customize the start workflow options
func (opts *FooWithBarOptions) WithStartWorkflow(swo client.StartWorkflowOptions) *FooWithBarOptions {
	opts.StartWorkflowOptions = &swo
	return opts
}

// FooWithBar executes a(n) test.client_task_queue.v1.ClientTaskQueueService.FooWithBar activity and blocks until completion
func FooWithBar(ctx workflow.Context, input *v1.FooInput, signal *v1.BarInput, opts ...*FooWithBarOptions) (*v1.FooOutput, error) {
	run, err := FooWithBarAsync(ctx, input, signal, opts...)
	if err != nil {
		return nil, err
	}
	return run.Get(ctx)
}

// FooWithBarAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.FooWithBar activity and returns a handle to the activity
func FooWithBarAsync(ctx workflow.Context, input *v1.FooInput, signal *v1.BarInput, opts ...*FooWithBarOptions) (FooRun, error) {
	activityName := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.FooWithBar")
	if activityName == "" {
		return nil, temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("no activity registered for %s", "test.client_task_queue.v1.ClientTaskQueueService.FooWithBar"),
			"Unimplemented",
			nil,
		)
	}

	var opt *FooWithBarOptions
	if len(opts) > 0 && opts[0] != nil {
		opt = opts[0]
	} else {
		opt = NewFooWithBarOptions()
	}
	ctx, req, err := opt.Build(ctx, input, signal)
	if err != nil {
		return nil, err
	}
	ctx, cancel := workflow.WithCancel(ctx)
	return &fooRun{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     req.GetStartWorkflowOptions().GetId(),
	}, nil
}

// FooWithBazOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update for a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow
type FooWithBazOptions struct {
	activityOptions   *workflow.ActivityOptions
	heartbeatInterval time.Duration
	updateOptions     *BazUpdateOptions
	parentClosePolicy enumsv1.ParentClosePolicy
	workflowOptions   *FooWorkflowOptions
}

// NewFooWithBazOptions initializes a new FooWithBazOptions value
func NewFooWithBazOptions() *FooWithBazOptions {
	return &FooWithBazOptions{}
}

// Build builds the activity context and input for an update with start workflow activity
func (o *FooWithBazOptions) Build(ctx workflow.Context, input *v1.FooInput, update *v1.BazInput) (workflow.Context, *xnsv1.UpdateWithStartRequest, error) {
	wo := o.workflowOptions
	if wo == nil {
		wo = NewFooWorkflowOptions()
	}

	_, swreq, err := wo.Build(ctx, input)
	if err != nil {
		return nil, nil, fmt.Errorf("error building start workflow options: %w", err)
	}

	uo := o.updateOptions
	if uo == nil {
		uo = NewBazUpdateOptions()
	}

	ctx, ureq, err := uo.Build(ctx, swreq.GetStartWorkflowOptions().GetId(), "", update)
	if err != nil {
		return nil, nil, fmt.Errorf("error building update options: %w", err)
	}

	var ao workflow.ActivityOptions
	if o.activityOptions != nil {
		ao = *o.activityOptions
	}

	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = 60000000000 // 1 minute
	}

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = time.Hour * 24
	}

	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	// configure heartbeat interval
	if o.heartbeatInterval == 0 {
		o.heartbeatInterval = 30000000000 // 30 seconds
	}

	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.UpdateWithStartRequest{
		HeartbeatInterval:     durationpb.New(o.heartbeatInterval),
		Input:                 swreq.GetRequest(),
		ParentClosePolicy:     convert.ToParentClosePolicy(o.parentClosePolicy),
		StartWorkflowOptions:  swreq.GetStartWorkflowOptions(),
		Update:                ureq.GetRequest(),
		UpdateWorkflowOptions: ureq.GetUpdateWorkflowOptions(),
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (o *FooWithBazOptions) WithActivityOptions(ao workflow.ActivityOptions) *FooWithBazOptions {
	o.activityOptions = &ao
	return o
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (o *FooWithBazOptions) WithHeartbeatInterval(d time.Duration) *FooWithBazOptions {
	o.heartbeatInterval = d
	return o
}

// WithParentClosePolicy can be used to customize the parent close policy for the workflow
func (o *FooWithBazOptions) WithParentClosePolicy(p enumsv1.ParentClosePolicy) *FooWithBazOptions {
	o.parentClosePolicy = p
	return o
}

// WithUpdateOptions can be used to customize the update options
func (o *FooWithBazOptions) WithUpdateOptions(uo *BazUpdateOptions) *FooWithBazOptions {
	o.updateOptions = uo
	return o
}

// WithWorkflowOptions can be used to customize the workflow options
func (o *FooWithBazOptions) WithWorkflowOptions(wo *FooWorkflowOptions) *FooWithBazOptions {
	o.workflowOptions = wo
	return o
}

// FooWithBaz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update for a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow, starting it if necessary, and blocks until error or update is complete
func FooWithBaz(ctx workflow.Context, input *v1.FooInput, update *v1.BazInput, options ...*FooWithBazOptions) (*v1.BazOutput, FooRun, error) {
	handle, run, err := FooWithBazAsync(ctx, input, update, options...)
	if err != nil {
		return nil, run, err
	}
	if out, err := handle.Get(ctx); err != nil {
		return nil, run, err
	} else {
		return out, run, nil
	}
}

// FooWithBazAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update for a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow, starting it if necessary, and returns a handle to the update and workflow execution
func FooWithBazAsync(ctx workflow.Context, input *v1.FooInput, update *v1.BazInput, options ...*FooWithBazOptions) (BazHandle, FooRun, error) {
	activityName := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.FooWithBaz")
	if activityName == "" {
		return nil, nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(fmt.Sprintf("no activity registered for %s", activityName), "Unimplemented", nil))
	}
	var o *FooWithBazOptions
	if len(options) > 0 && options[0] != nil {
		o = options[0]
	} else {
		o = NewFooWithBazOptions()
	}
	ctx, req, err := o.Build(ctx, input, update)
	if err != nil {
		return nil, nil, clientTaskQueueServiceOptions.convertError(err)
	}
	var parentClosePolicy enumsv1.ParentClosePolicy
	if p := req.GetParentClosePolicy(); p != temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_UNSPECIFIED {
		parentClosePolicy = enumsv1.ParentClosePolicy(p)
	}
	ctx, cancel := workflow.WithCancel(ctx)
	handle := &bazHandle{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     req.GetUpdateWorkflowOptions().GetUpdateId(),
	}
	run := &fooRun{
		cancel:            cancel,
		ctx:               ctx,
		heartbeatInterval: req.GetHeartbeatInterval().AsDuration(),
		id:                req.GetStartWorkflowOptions().GetId(),
		parentClosePolicy: parentClosePolicy,
	}
	return handle, run, nil
}

// QuxWorkflowOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow execution
type QuxWorkflowOptions struct {
	ActivityOptions      *workflow.ActivityOptions
	Detached             bool
	HeartbeatInterval    time.Duration
	HeartbeatTimeout     time.Duration
	ParentClosePolicy    enumsv1.ParentClosePolicy
	StartWorkflowOptions *client.StartWorkflowOptions
}

// NewQuxWorkflowOptions initializes a new QuxWorkflowOptions value
func NewQuxWorkflowOptions() *QuxWorkflowOptions {
	return &QuxWorkflowOptions{}
}

// Build initializes the activity context and input
func (opts *QuxWorkflowOptions) Build(ctx workflow.Context, input *v1.QuxInput) (workflow.Context, *xnsv1.WorkflowRequest, error) {
	// initialize start workflow options
	swo := client.StartWorkflowOptions{}
	if opts.StartWorkflowOptions != nil {
		swo = *opts.StartWorkflowOptions
	}

	// initialize workflow id if not set
	if swo.ID == "" {
		if err := workflow.SideEffect(ctx, func(ctx workflow.Context) any {
			id, err := uuid.NewRandom()
			if err != nil {
				workflow.GetLogger(ctx).Error("error generating workflow id", "error", err)
				return nil
			}
			return id
		}).Get(&swo.ID); err != nil {
			return nil, nil, err
		}
	}
	if swo.ID == "" {
		return nil, nil, temporal.NewNonRetryableApplicationError("workflow id is required", "InvalidArgument", nil)
	}

	// marshal workflow request protobuf message
	inputpb, err := anypb.New(input)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling workflow request: %w", err)
	}

	// marshal start workflow options protobuf message
	swopb, err := xns.MarshalStartWorkflowOptions(swo)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling start workflow options: %w", err)
	}

	// marshal parent close policy protobuf message
	var parentClosePolicy temporalv1.ParentClosePolicy
	switch opts.ParentClosePolicy {
	case enumsv1.PARENT_CLOSE_POLICY_ABANDON:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_ABANDON
	case enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL
	case enumsv1.PARENT_CLOSE_POLICY_TERMINATE:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE
	}

	// initialize xns activity options
	ao := workflow.ActivityOptions{}
	if opts.ActivityOptions != nil {
		ao = *opts.ActivityOptions
	}

	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = time.Second * 60
	}

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = time.Hour * 24
	}

	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	// configure heartbeat interval
	if opts.HeartbeatInterval == 0 {
		opts.HeartbeatInterval = ao.HeartbeatTimeout / 2
	}

	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.WorkflowRequest{
		Detached:             opts.Detached,
		HeartbeatInterval:    durationpb.New(opts.HeartbeatInterval),
		ParentClosePolicy:    parentClosePolicy,
		Request:              inputpb,
		StartWorkflowOptions: swopb,
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (opts *QuxWorkflowOptions) WithActivityOptions(ao workflow.ActivityOptions) *QuxWorkflowOptions {
	opts.ActivityOptions = &ao
	return opts
}

// WithDetached can be used to start a workflow execution and exit immediately
func (opts *QuxWorkflowOptions) WithDetached(d bool) *QuxWorkflowOptions {
	opts.Detached = d
	return opts
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (opts *QuxWorkflowOptions) WithHeartbeatInterval(d time.Duration) *QuxWorkflowOptions {
	opts.HeartbeatInterval = d
	return opts
}

// WithHeartbeatTimeout can be used to customize the activity heartbeat timeout
func (opts *QuxWorkflowOptions) WithHeartbeatTimeout(d time.Duration) *QuxWorkflowOptions {
	opts.HeartbeatTimeout = d
	return opts
}

// WithParentClosePolicy can be used to customize the cancellation propagation behavior
func (opts *QuxWorkflowOptions) WithParentClosePolicy(policy enumsv1.ParentClosePolicy) *QuxWorkflowOptions {
	opts.ParentClosePolicy = policy
	return opts
}

// WithStartWorkflowOptions can be used to customize the start workflow options
func (opts *QuxWorkflowOptions) WithStartWorkflow(swo client.StartWorkflowOptions) *QuxWorkflowOptions {
	opts.StartWorkflowOptions = &swo
	return opts
}

// QuxRun provides a handle to a test.client_task_queue.v1.ClientTaskQueueService.Qux workflow execution
type QuxRun interface {
	// Cancel cancels the workflow
	Cancel(workflow.Context) error

	// Future returns the inner workflow.Future
	Future() workflow.Future

	// Get returns the inner workflow.Future
	Get(workflow.Context) (*v1.QuxOutput, error)

	// ID returns the workflow id
	ID() string

	// Bar executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal and blocks until completion
	Bar(workflow.Context, *v1.BarInput, ...*BarSignalOptions) error

	// BarAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal and returns a handle to the underlying activity
	BarAsync(workflow.Context, *v1.BarInput, ...*BarSignalOptions) (BarSignalHandle, error)

	// Baz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and blocks until completion
	Baz(workflow.Context, *v1.BazInput, ...*BazUpdateOptions) (*v1.BazOutput, error)

	// BazAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and returns a handle to the underlying activity
	BazAsync(workflow.Context, *v1.BazInput, ...*BazUpdateOptions) (BazHandle, error)
}

// quxRun provides a(n) QuxRun implementation
type quxRun struct {
	cancel            func()
	ctx               workflow.Context
	future            workflow.Future
	id                string
	heartbeatInterval time.Duration
	parentClosePolicy enumsv1.ParentClosePolicy
}

// Cancel the underlying workflow execution
func (r *quxRun) Cancel(ctx workflow.Context) error {
	if r.cancel != nil {
		r.cancel()
		if _, err := r.Get(ctx); err != nil && !errors.Is(err, workflow.ErrCanceled) {
			return err
		}
		return nil
	}
	return CancelClientTaskQueueServiceWorkflow(ctx, r.id, "")
}

// Future returns the underlying activity future
func (r *quxRun) Future() workflow.Future {
	if r.future == nil {
		rr := GetQuxAsync(r.ctx, r.id, "").(*quxRun)
		r.future = rr.future
		r.cancel = rr.cancel
	}
	return r.future
}

// Get blocks on activity completion and returns the underlying workflow result
func (r *quxRun) Get(ctx workflow.Context) (*v1.QuxOutput, error) {
	ctx, cancel := workflow.WithCancel(ctx)
	if r.future == nil {
		rr := GetQuxAsync(ctx, r.id, "", NewGetQuxOptions().WithParentClosePolicy(r.parentClosePolicy).WithHeartbeatInterval(r.heartbeatInterval)).(*quxRun)
		r.future = rr.future
		r.cancel = cancel
	}
	var resp v1.QuxOutput
	if err := r.future.Get(ctx, &resp); err != nil {
		return nil, err
	}
	return &resp, nil
}

// ID returns the underlying workflow id
func (r *quxRun) ID() string {
	return r.id
}

// Bar executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal and blocks until the underlying activity completes
func (r *quxRun) Bar(ctx workflow.Context, req *v1.BarInput, opts ...*BarSignalOptions) error {
	return Bar(ctx, r.ID(), "", req, opts...)
}

// BarAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal and returns a handle to the underlying activity
func (r *quxRun) BarAsync(ctx workflow.Context, req *v1.BarInput, opts ...*BarSignalOptions) (BarSignalHandle, error) {
	return BarAsync(ctx, r.ID(), "", req, opts...)
}

// Baz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and blocks until completion
func (r *quxRun) Baz(ctx workflow.Context, req *v1.BazInput, opts ...*BazUpdateOptions) (*v1.BazOutput, error) {
	return Baz(ctx, r.ID(), "", req, opts...)
}

// BazAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and returns a handle to the underlying activity
func (r *quxRun) BazAsync(ctx workflow.Context, req *v1.BazInput, opts ...*BazUpdateOptions) (BazHandle, error) {
	return BazAsync(ctx, r.ID(), "", req, opts...)
}

// Qux executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow and blocks until error or response is received
func Qux(ctx workflow.Context, req *v1.QuxInput, opts ...*QuxWorkflowOptions) (*v1.QuxOutput, error) {
	run, err := QuxAsync(ctx, req, opts...)
	if err != nil {
		return nil, err
	}
	return run.Get(ctx)
}

// QuxAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow and returns a handle to the underlying activity
func QuxAsync(ctx workflow.Context, input *v1.QuxInput, opts ...*QuxWorkflowOptions) (QuxRun, error) {
	activityName := clientTaskQueueServiceOptions.filterActivity(v1.QuxWorkflowName)
	if activityName == "" {
		return nil, temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("no activity registered for %s", v1.QuxWorkflowName),
			"Unimplemented",
			nil,
		)
	}

	var opt *QuxWorkflowOptions
	if len(opts) > 0 && opts[0] != nil {
		opt = opts[0]
	} else {
		opt = NewQuxWorkflowOptions()
	}
	ctx, req, err := opt.Build(ctx, input)
	if err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(err)
	}
	ctx, cancel := workflow.WithCancel(ctx)
	return &quxRun{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     req.GetStartWorkflowOptions().GetId(),
	}, nil
}

// GetQux returns a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow execution
func GetQux(ctx workflow.Context, workflowID string, runID string, options ...*GetQuxOptions) (out *v1.QuxOutput, err error) {
	out, err = GetQuxAsync(ctx, workflowID, runID, options...).Get(ctx)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// GetQuxAsync returns a handle to a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow execution
func GetQuxAsync(ctx workflow.Context, workflowID string, runID string, options ...*GetQuxOptions) QuxRun {
	activityName := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.GetQux")
	if activityName == "" {
		f, set := workflow.NewFuture(ctx)
		set.SetError(temporal.NewNonRetryableApplicationError(fmt.Sprintf("no activity registered for %s", activityName), "Unimplemented", nil))
		return &quxRun{
			future: f,
			id:     workflowID,
		}
	}
	var opt *GetQuxOptions
	if len(options) > 0 && options[0] != nil {
		opt = options[0]
	} else {
		opt = NewGetQuxOptions()
	}
	ctx, req, err := opt.Build(ctx, workflowID, runID)
	if err != nil {
		f, set := workflow.NewFuture(ctx)
		set.SetError(clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(fmt.Sprintf("no activity registered for %s", activityName), "Unimplemented", nil)))
		return &quxRun{
			future: f,
			id:     workflowID,
		}
	}
	ctx, cancel := workflow.WithCancel(ctx)
	return &quxRun{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     workflowID,
	}
}

// GetQuxOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow execution getter activity
type GetQuxOptions struct {
	activityOptions   *workflow.ActivityOptions
	heartbeatInterval time.Duration
	parentClosePolicy enumsv1.ParentClosePolicy
}

// NewGetQuxOptions initializes a new GetQuxOptions value
func NewGetQuxOptions() *GetQuxOptions {
	return &GetQuxOptions{}
}

// Build initializes the activity context and input
func (opt *GetQuxOptions) Build(ctx workflow.Context, workflowID string, runID string) (workflow.Context, *xnsv1.GetWorkflowRequest, error) {
	if opt.heartbeatInterval == 0 {
		opt.heartbeatInterval = 30000000000 // 30 seconds
	}

	// configure activity options
	var ao workflow.ActivityOptions
	if opt.activityOptions != nil {
		ao = *opt.activityOptions
	} else {
		ao = workflow.ActivityOptions{}
	}
	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = 60000000000 // 1 minute
	}
	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = 86400000000000 // 1 day
	}
	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.GetWorkflowRequest{
		HeartbeatInterval: durationpb.New(opt.heartbeatInterval),
		ParentClosePolicy: opt.parentClosePolicy,
		RunId:             runID,
		WorkflowId:        workflowID,
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (o *GetQuxOptions) WithActivityOptions(ao workflow.ActivityOptions) *GetQuxOptions {
	o.activityOptions = &ao
	return o
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (o *GetQuxOptions) WithHeartbeatInterval(d time.Duration) *GetQuxOptions {
	o.heartbeatInterval = d
	return o
}

// WithParentClosePolicy can be used to customize the cancellation propagation behavior
func (o *GetQuxOptions) WithParentClosePolicy(policy enumsv1.ParentClosePolicy) *GetQuxOptions {
	o.parentClosePolicy = policy
	return o
}

// QuxWithBarOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.QuxWithBar activity
type QuxWithBarOptions struct {
	ActivityOptions      *workflow.ActivityOptions
	Detached             bool
	HeartbeatInterval    time.Duration
	HeartbeatTimeout     time.Duration
	ParentClosePolicy    enumsv1.ParentClosePolicy
	StartWorkflowOptions *client.StartWorkflowOptions
}

// NewQuxWithBarOptions initializes a new QuxWithBarOptions value
func NewQuxWithBarOptions() *QuxWithBarOptions {
	return &QuxWithBarOptions{}
}

// Build initializes the activity context and input
func (opts *QuxWithBarOptions) Build(ctx workflow.Context, input *v1.QuxInput, signal *v1.BarInput) (workflow.Context, *xnsv1.WorkflowRequest, error) {
	// initialize start workflow options
	swo := client.StartWorkflowOptions{}
	if opts.StartWorkflowOptions != nil {
		swo = *opts.StartWorkflowOptions
	}

	// initialize workflow id if not set
	if swo.ID == "" {
		if err := workflow.SideEffect(ctx, func(ctx workflow.Context) any {
			id, err := uuid.NewRandom()
			if err != nil {
				workflow.GetLogger(ctx).Error("error generating workflow id", "error", err)
				return nil
			}
			return id
		}).Get(&swo.ID); err != nil {
			return nil, nil, err
		}
	}
	if swo.ID == "" {
		return nil, nil, temporal.NewNonRetryableApplicationError("workflow id is required", "InvalidArgument", nil)
	}

	// marshal workflow request protobuf message
	inputpb, err := anypb.New(input)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling workflow request: %w", err)
	}

	// marshal signal request protobuf message
	signalpb, err := anypb.New(signal)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling signal request: %w", err)
	}

	// marshal start workflow options protobuf message
	swopb, err := xns.MarshalStartWorkflowOptions(swo)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling start workflow options: %w", err)
	}

	// marshal parent close policy protobuf message
	var parentClosePolicy temporalv1.ParentClosePolicy
	switch opts.ParentClosePolicy {
	case enumsv1.PARENT_CLOSE_POLICY_ABANDON:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_ABANDON
	case enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL
	case enumsv1.PARENT_CLOSE_POLICY_TERMINATE:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE
	}

	// initialize xns activity options
	ao := workflow.ActivityOptions{}
	if opts.ActivityOptions != nil {
		ao = *opts.ActivityOptions
	}

	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = time.Second * 60
	}

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = time.Hour * 24
	}

	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	// configure heartbeat interval
	if opts.HeartbeatInterval == 0 {
		opts.HeartbeatInterval = ao.HeartbeatTimeout / 2
	}

	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.WorkflowRequest{
		Detached:             opts.Detached,
		HeartbeatInterval:    durationpb.New(opts.HeartbeatInterval),
		ParentClosePolicy:    parentClosePolicy,
		Request:              inputpb,
		Signal:               signalpb,
		StartWorkflowOptions: swopb,
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (opts *QuxWithBarOptions) WithActivityOptions(ao workflow.ActivityOptions) *QuxWithBarOptions {
	opts.ActivityOptions = &ao
	return opts
}

// WithDetached can be used to start a workflow execution and exit immediately
func (opts *QuxWithBarOptions) WithDetached(d bool) *QuxWithBarOptions {
	opts.Detached = d
	return opts
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (opts *QuxWithBarOptions) WithHeartbeatInterval(d time.Duration) *QuxWithBarOptions {
	opts.HeartbeatInterval = d
	return opts
}

// WithHeartbeatTimeout can be used to customize the activity heartbeat timeout
func (opts *QuxWithBarOptions) WithHeartbeatTimeout(d time.Duration) *QuxWithBarOptions {
	opts.HeartbeatTimeout = d
	return opts
}

// WithParentClosePolicy can be used to customize the cancellation propagation behavior
func (opts *QuxWithBarOptions) WithParentClosePolicy(policy enumsv1.ParentClosePolicy) *QuxWithBarOptions {
	opts.ParentClosePolicy = policy
	return opts
}

// WithStartWorkflowOptions can be used to customize the start workflow options
func (opts *QuxWithBarOptions) WithStartWorkflow(swo client.StartWorkflowOptions) *QuxWithBarOptions {
	opts.StartWorkflowOptions = &swo
	return opts
}

// QuxWithBar executes a(n) test.client_task_queue.v1.ClientTaskQueueService.QuxWithBar activity and blocks until completion
func QuxWithBar(ctx workflow.Context, input *v1.QuxInput, signal *v1.BarInput, opts ...*QuxWithBarOptions) (*v1.QuxOutput, error) {
	run, err := QuxWithBarAsync(ctx, input, signal, opts...)
	if err != nil {
		return nil, err
	}
	return run.Get(ctx)
}

// QuxWithBarAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.QuxWithBar activity and returns a handle to the activity
func QuxWithBarAsync(ctx workflow.Context, input *v1.QuxInput, signal *v1.BarInput, opts ...*QuxWithBarOptions) (QuxRun, error) {
	activityName := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.QuxWithBar")
	if activityName == "" {
		return nil, temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("no activity registered for %s", "test.client_task_queue.v1.ClientTaskQueueService.QuxWithBar"),
			"Unimplemented",
			nil,
		)
	}

	var opt *QuxWithBarOptions
	if len(opts) > 0 && opts[0] != nil {
		opt = opts[0]
	} else {
		opt = NewQuxWithBarOptions()
	}
	ctx, req, err := opt.Build(ctx, input, signal)
	if err != nil {
		return nil, err
	}
	ctx, cancel := workflow.WithCancel(ctx)
	return &quxRun{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     req.GetStartWorkflowOptions().GetId(),
	}, nil
}

// QuxWithBazOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update for a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow
type QuxWithBazOptions struct {
	activityOptions   *workflow.ActivityOptions
	heartbeatInterval time.Duration
	updateOptions     *BazUpdateOptions
	parentClosePolicy enumsv1.ParentClosePolicy
	workflowOptions   *QuxWorkflowOptions
}

// NewQuxWithBazOptions initializes a new QuxWithBazOptions value
func NewQuxWithBazOptions() *QuxWithBazOptions {
	return &QuxWithBazOptions{}
}

// Build builds the activity context and input for an update with start workflow activity
func (o *QuxWithBazOptions) Build(ctx workflow.Context, input *v1.QuxInput, update *v1.BazInput) (workflow.Context, *xnsv1.UpdateWithStartRequest, error) {
	wo := o.workflowOptions
	if wo == nil {
		wo = NewQuxWorkflowOptions()
	}

	_, swreq, err := wo.Build(ctx, input)
	if err != nil {
		return nil, nil, fmt.Errorf("error building start workflow options: %w", err)
	}

	uo := o.updateOptions
	if uo == nil {
		uo = NewBazUpdateOptions()
	}

	ctx, ureq, err := uo.Build(ctx, swreq.GetStartWorkflowOptions().GetId(), "", update)
	if err != nil {
		return nil, nil, fmt.Errorf("error building update options: %w", err)
	}

	var ao workflow.ActivityOptions
	if o.activityOptions != nil {
		ao = *o.activityOptions
	}

	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = 60000000000 // 1 minute
	}

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = time.Hour * 24
	}

	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	// configure heartbeat interval
	if o.heartbeatInterval == 0 {
		o.heartbeatInterval = 30000000000 // 30 seconds
	}

	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.UpdateWithStartRequest{
		HeartbeatInterval:     durationpb.New(o.heartbeatInterval),
		Input:                 swreq.GetRequest(),
		ParentClosePolicy:     convert.ToParentClosePolicy(o.parentClosePolicy),
		StartWorkflowOptions:  swreq.GetStartWorkflowOptions(),
		Update:                ureq.GetRequest(),
		UpdateWorkflowOptions: ureq.GetUpdateWorkflowOptions(),
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (o *QuxWithBazOptions) WithActivityOptions(ao workflow.ActivityOptions) *QuxWithBazOptions {
	o.activityOptions = &ao
	return o
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (o *QuxWithBazOptions) WithHeartbeatInterval(d time.Duration) *QuxWithBazOptions {
	o.heartbeatInterval = d
	return o
}

// WithParentClosePolicy can be used to customize the parent close policy for the workflow
func (o *QuxWithBazOptions) WithParentClosePolicy(p enumsv1.ParentClosePolicy) *QuxWithBazOptions {
	o.parentClosePolicy = p
	return o
}

// WithUpdateOptions can be used to customize the update options
func (o *QuxWithBazOptions) WithUpdateOptions(uo *BazUpdateOptions) *QuxWithBazOptions {
	o.updateOptions = uo
	return o
}

// WithWorkflowOptions can be used to customize the workflow options
func (o *QuxWithBazOptions) WithWorkflowOptions(wo *QuxWorkflowOptions) *QuxWithBazOptions {
	o.workflowOptions = wo
	return o
}

// QuxWithBaz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update for a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow, starting it if necessary, and blocks until error or update is complete
func QuxWithBaz(ctx workflow.Context, input *v1.QuxInput, update *v1.BazInput, options ...*QuxWithBazOptions) (*v1.BazOutput, QuxRun, error) {
	handle, run, err := QuxWithBazAsync(ctx, input, update, options...)
	if err != nil {
		return nil, run, err
	}
	if out, err := handle.Get(ctx); err != nil {
		return nil, run, err
	} else {
		return out, run, nil
	}
}

// QuxWithBazAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update for a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow, starting it if necessary, and returns a handle to the update and workflow execution
func QuxWithBazAsync(ctx workflow.Context, input *v1.QuxInput, update *v1.BazInput, options ...*QuxWithBazOptions) (BazHandle, QuxRun, error) {
	activityName := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.QuxWithBaz")
	if activityName == "" {
		return nil, nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(fmt.Sprintf("no activity registered for %s", activityName), "Unimplemented", nil))
	}
	var o *QuxWithBazOptions
	if len(options) > 0 && options[0] != nil {
		o = options[0]
	} else {
		o = NewQuxWithBazOptions()
	}
	ctx, req, err := o.Build(ctx, input, update)
	if err != nil {
		return nil, nil, clientTaskQueueServiceOptions.convertError(err)
	}
	var parentClosePolicy enumsv1.ParentClosePolicy
	if p := req.GetParentClosePolicy(); p != temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_UNSPECIFIED {
		parentClosePolicy = enumsv1.ParentClosePolicy(p)
	}
	ctx, cancel := workflow.WithCancel(ctx)
	handle := &bazHandle{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     req.GetUpdateWorkflowOptions().GetUpdateId(),
	}
	run := &quxRun{
		cancel:            cancel,
		ctx:               ctx,
		heartbeatInterval: req.GetHeartbeatInterval().AsDuration(),
		id:                req.GetStartWorkflowOptions().GetId(),
		parentClosePolicy: parentClosePolicy,
	}
	return handle, run, nil
}

// BarSignalOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal execution
type BarSignalOptions struct {
	ActivityOptions   *workflow.ActivityOptions
	HeartbeatInterval time.Duration
}

// NewBarSignalOptions initializes a new BarSignalOptions value
func NewBarSignalOptions() *BarSignalOptions {
	return &BarSignalOptions{}
}

// WithActivityOptions can be used to customize the activity options
func (opts *BarSignalOptions) WithActivityOptions(ao workflow.ActivityOptions) *BarSignalOptions {
	opts.ActivityOptions = &ao
	return opts
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (opts *BarSignalOptions) WithHeartbeatInterval(d time.Duration) *BarSignalOptions {
	opts.HeartbeatInterval = d
	return opts
}

// BarSignalHandle provides a handle for a test.client_task_queue.v1.ClientTaskQueueService.Bar signal activity
type BarSignalHandle interface {
	// Cancel cancels the workflow
	Cancel(workflow.Context) error
	// Future returns the inner workflow.Future
	Future() workflow.Future
	// Get returns the inner workflow.Future
	Get(workflow.Context) error
}

// barSignalHandle provides a(n) BarQueryHandle implementation
type barSignalHandle struct {
	cancel func()
	future workflow.Future
}

// Cancel the underlying signal activity
func (r *barSignalHandle) Cancel(ctx workflow.Context) error {
	r.cancel()
	if err := r.Get(ctx); err != nil && !errors.Is(err, workflow.ErrCanceled) {
		return err
	}
	return nil
}

// Future returns the underlying activity future
func (r *barSignalHandle) Future() workflow.Future {
	return r.future
}

// Get blocks on activity completion
func (r *barSignalHandle) Get(ctx workflow.Context) error {
	return r.future.Get(ctx, nil)
}

// Bar executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal
func Bar(ctx workflow.Context, workflowID string, runID string, req *v1.BarInput, opts ...*BarSignalOptions) error {
	handle, err := BarAsync(ctx, workflowID, runID, req, opts...)
	if err != nil {
		return err
	}
	return handle.Get(ctx)
}

// BarAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal
func BarAsync(ctx workflow.Context, workflowID string, runID string, req *v1.BarInput, opts ...*BarSignalOptions) (BarSignalHandle, error) {
	activityName := clientTaskQueueServiceOptions.filterActivity(v1.BarSignalName)
	if activityName == "" {
		return nil, temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("no activity registered for %s", v1.BarSignalName),
			"Unimplemented",
			nil,
		)
	}

	opt := &BarSignalOptions{}
	if len(opts) > 0 && opts[0] != nil {
		opt = opts[0]
	}

	if opt.HeartbeatInterval == 0 {
		opt.HeartbeatInterval = time.Second * 30
	}

	// configure activity options
	var ao workflow.ActivityOptions
	if opt.ActivityOptions != nil {
		ao = *opt.ActivityOptions
	} else {
		ao = workflow.ActivityOptions{}
	}
	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = opt.HeartbeatInterval * 2
	}
	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = 60000000000 // 1 minute
	}
	ctx = workflow.WithActivityOptions(ctx, ao)

	// marshal workflow request
	wreq, err := anypb.New(req)
	if err != nil {
		return nil, fmt.Errorf("error marshalling workflow request: %w", err)
	}

	ctx, cancel := workflow.WithCancel(ctx)
	return &barSignalHandle{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, &xnsv1.SignalRequest{
			HeartbeatInterval: durationpb.New(opt.HeartbeatInterval),
			WorkflowId:        workflowID,
			RunId:             runID,
			Request:           wreq,
		}),
	}, nil
}

// BazUpdateOptions are used to configure a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update execution
type BazUpdateOptions struct {
	ActivityOptions       *workflow.ActivityOptions
	HeartbeatInterval     time.Duration
	UpdateWorkflowOptions *client.UpdateWorkflowOptions
}

// NewBazUpdateOptions initializes a new BazUpdateOptions value
func NewBazUpdateOptions() *BazUpdateOptions {
	return &BazUpdateOptions{}
}

// Build initializes the update options
func (opt *BazUpdateOptions) Build(ctx workflow.Context, workflowID string, runID string, input *v1.BazInput) (workflow.Context, *xnsv1.UpdateRequest, error) {
	// configure activity options
	var ao workflow.ActivityOptions
	if opt.ActivityOptions != nil {
		ao = *opt.ActivityOptions
	} else {
		ao = workflow.ActivityOptions{}
	}
	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = 60000000000 // 1 minute
	}
	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = time.Hour * 24
	}

	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	// configure heartbeat interval
	if opt.HeartbeatInterval == 0 {
		opt.HeartbeatInterval = 30000000000 // 30 seconds
	}

	ctx = workflow.WithActivityOptions(ctx, ao)

	uo := client.UpdateWorkflowOptions{}
	if opt.UpdateWorkflowOptions != nil {
		uo = *opt.UpdateWorkflowOptions
	}
	uo.WorkflowID = workflowID
	uo.RunID = runID
	if uo.UpdateID == "" {
		if err := workflow.SideEffect(ctx, func(ctx workflow.Context) any {
			id, err := uuid.NewRandom()
			if err != nil {
				workflow.GetLogger(ctx).Error("error generating update id", "error", err)
				return nil
			}
			return id
		}).Get(&uo.UpdateID); err != nil {
			return nil, nil, err
		}
	}
	if uo.UpdateID == "" {
		return nil, nil, temporal.NewNonRetryableApplicationError("update id is required", "InvalidArgument", nil)
	}

	uopb, err := xns.MarshalUpdateWorkflowOptions(uo)
	if err != nil {
		return nil, nil, fmt.Errorf("error marshalling update workflow options: %w", err)
	}

	inpb, err := anypb.New(input)
	if err != nil {
		return nil, nil, fmt.Errorf("error marshalling update request: %w", err)
	}

	return ctx, &xnsv1.UpdateRequest{
		HeartbeatInterval:     durationpb.New(opt.HeartbeatInterval),
		Request:               inpb,
		UpdateWorkflowOptions: uopb,
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (opts *BazUpdateOptions) WithActivityOptions(ao workflow.ActivityOptions) *BazUpdateOptions {
	opts.ActivityOptions = &ao
	return opts
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (opts *BazUpdateOptions) WithHeartbeatInterval(d time.Duration) *BazUpdateOptions {
	opts.HeartbeatInterval = d
	return opts
}

// WithUpdateWorkflowOptions can be used to customize the update workflow options
func (opts *BazUpdateOptions) WithUpdateWorkflowOptions(uwo client.UpdateWorkflowOptions) *BazUpdateOptions {
	opts.UpdateWorkflowOptions = &uwo
	return opts
}

// BazHandle provides a handle to a test.client_task_queue.v1.ClientTaskQueueService.Baz workflow update
type BazHandle interface {
	// Cancel cancels the update activity
	Cancel(workflow.Context) error

	// Future returns the inner workflow.Future
	Future() workflow.Future

	// Get blocks on update completion and returns the result
	Get(workflow.Context) (*v1.BazOutput, error)

	// ID returns the update id
	ID() string
}

// bazHandle provides a(n) BazHandle implementation
type bazHandle struct {
	cancel func()
	future workflow.Future
	id     string
}

// Cancel the underlying workflow update
func (r *bazHandle) Cancel(ctx workflow.Context) error {
	r.cancel()
	if _, err := r.Get(ctx); err != nil && !errors.Is(err, workflow.ErrCanceled) {
		return err
	}
	return nil
}

// Future returns the underlying activity future
func (r *bazHandle) Future() workflow.Future {
	return r.future
}

// Get blocks on activity completion and returns the underlying update result
func (r *bazHandle) Get(ctx workflow.Context) (*v1.BazOutput, error) {
	var resp v1.BazOutput
	if err := r.future.Get(ctx, &resp); err != nil {
		return nil, err
	}
	return &resp, nil
}

// ID returns the underlying workflow id
func (r *bazHandle) ID() string {
	return r.id
}

// Baz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and blocks until error or response received
func Baz(ctx workflow.Context, workflowID string, runID string, req *v1.BazInput, opts ...*BazUpdateOptions) (*v1.BazOutput, error) {
	run, err := BazAsync(ctx, workflowID, runID, req, opts...)
	if err != nil {
		return nil, err
	}
	return run.Get(ctx)
}

// BazAsync executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update and blocks until error or response received
func BazAsync(ctx workflow.Context, workflowID string, runID string, input *v1.BazInput, opts ...*BazUpdateOptions) (BazHandle, error) {
	activityName := clientTaskQueueServiceOptions.filterActivity(v1.BazUpdateName)
	if activityName == "" {
		return nil, temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("no activity registered for %s", v1.BazUpdateName),
			"Unimplemented",
			nil,
		)
	}

	var opt *BazUpdateOptions
	if len(opts) > 0 && opts[0] != nil {
		opt = opts[0]
	} else {
		opt = NewBazUpdateOptions()
	}

	ctx, req, err := opt.Build(ctx, workflowID, runID, input)
	if err != nil {
		return nil, err
	}
	ctx, cancel := workflow.WithCancel(ctx)
	return &bazHandle{
		cancel: cancel,
		id:     req.GetUpdateWorkflowOptions().GetUpdateId(),
		future: workflow.ExecuteActivity(ctx, activityName, req),
	}, nil
}

// CancelClientTaskQueueServiceWorkflow cancels an existing workflow
func CancelClientTaskQueueServiceWorkflow(ctx workflow.Context, workflowID string, runID string) error {
	return CancelClientTaskQueueServiceWorkflowAsync(ctx, workflowID, runID).Get(ctx, nil)
}

// CancelClientTaskQueueServiceWorkflowAsync cancels an existing workflow
func CancelClientTaskQueueServiceWorkflowAsync(ctx workflow.Context, workflowID string, runID string) workflow.Future {
	activityName := clientTaskQueueServiceOptions.filterActivity("test.client_task_queue.v1.ClientTaskQueueService.CancelWorkflow")
	if activityName == "" {
		f, s := workflow.NewFuture(ctx)
		s.SetError(temporal.NewNonRetryableApplicationError(
			"no activity registered for test.client_task_queue.v1.ClientTaskQueueService.CancelWorkflow",
			"Unimplemented",
			nil,
		))
		return f
	}
	ao := workflow.GetActivityOptions(ctx)
	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.StartToCloseTimeout = time.Minute
	}
	ctx = workflow.WithActivityOptions(ctx, ao)
	return workflow.ExecuteActivity(ctx, activityName, workflowID, runID)
}

// clientTaskQueueServiceActivities provides activities that can be used to interact with a(n) ClientTaskQueueService service's workflow, queries, signals, and updates across namespaces
type clientTaskQueueServiceActivities struct {
	client v1.ClientTaskQueueServiceClient
}

// CancelWorkflow cancels an existing workflow execution
func (a *clientTaskQueueServiceActivities) CancelWorkflow(ctx context.Context, workflowID string, runID string) error {
	return a.client.CancelWorkflow(ctx, workflowID, runID)
}

// GetFoo retrieves a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow via an activity
func (a *clientTaskQueueServiceActivities) GetFoo(ctx context.Context, input *xnsv1.GetWorkflowRequest) (out *v1.FooOutput, err error) {
	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Second * 30
	}

	actx, cancel := context.WithCancel(context.Background())
	defer cancel()
	run := a.client.GetFoo(actx, input.GetWorkflowId(), input.GetRunId())
	done := make(chan struct{})
	go func() {
		defer close(done)
		out, err = run.Get(actx)
	}()

	for {
		select {
		// send heartbeats periodically
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx)

		// return retryable error if the worker is stopping
		case <-activity.GetWorkerStopChannel(ctx):
			return nil, clientTaskQueueServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopped"))

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				activity.GetLogger(ctx).Info("worker is stopping")
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopped"))
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == enumsv1.PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(context.Background(), time.Minute)
					defer cancel()
					if parentClosePolicy == enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, clientTaskQueueServiceOptions.convertError(err)
					}
				}
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		// handle workflow completion
		case <-done:
			return out, clientTaskQueueServiceOptions.convertError(err)
		}
	}
}

// Foo executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow via an activity
func (a *clientTaskQueueServiceActivities) Foo(ctx context.Context, input *xnsv1.WorkflowRequest) (resp *v1.FooOutput, err error) {
	// unmarshal workflow request
	var req v1.FooInput
	if err := input.Request.UnmarshalTo(&req); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling workflow request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.FooInput", input.Request.GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// initialize workflow execution
	actx := ctx
	if !input.GetDetached() {
		var cancel context.CancelFunc
		actx, cancel = context.WithCancel(context.Background())
		defer cancel()
	}
	var run v1.FooRun
	run, err = a.client.FooAsync(actx, &req, v1.NewFooOptions().WithStartWorkflowOptions(
		xns.UnmarshalStartWorkflowOptions(input.GetStartWorkflowOptions()),
	))
	if err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(err)
	}

	// exit early if detached enabled
	if input.GetDetached() {
		return nil, nil
	}

	// otherwise, wait for execution to complete in child goroutine
	doneCh := make(chan struct{})
	go func() {
		resp, err = run.Get(actx)
		close(doneCh)
	}()

	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Second * 30
	}

	// heartbeat activity while waiting for workflow execution to complete
	for {
		select {
		// send heartbeats periodically
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx, run.ID())

		// return retryable error on worker close
		case <-activity.GetWorkerStopChannel(ctx):
			return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(context.Background(), time.Minute)
					defer cancel()
					if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, clientTaskQueueServiceOptions.convertError(err)
					}
				}
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		// handle workflow completion
		case <-doneCh:
			return resp, clientTaskQueueServiceOptions.convertError(err)
		}
	}
}

// FooWithBar sends a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal to a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow via an activity
func (a *clientTaskQueueServiceActivities) FooWithBar(ctx context.Context, input *xnsv1.WorkflowRequest) (resp *v1.FooOutput, err error) {
	// unmarshal workflow request
	var req v1.FooInput
	if err := input.Request.UnmarshalTo(&req); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling workflow request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.FooInput", input.Request.GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// unmarshal signal request
	var signal v1.BarInput
	if err := input.Signal.UnmarshalTo(&signal); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling signal request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.BarInput", input.Signal.GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// initialize workflow execution
	actx := ctx
	if !input.GetDetached() {
		var cancel context.CancelFunc
		actx, cancel = context.WithCancel(context.Background())
		defer cancel()
	}
	var run v1.FooRun
	run, err = a.client.FooWithBarAsync(actx, &req, &signal, v1.NewFooOptions().WithStartWorkflowOptions(
		xns.UnmarshalStartWorkflowOptions(input.GetStartWorkflowOptions()),
	))
	if err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(err)
	}

	// exit early if detached enabled
	if input.GetDetached() {
		return nil, nil
	}

	// otherwise, wait for execution to complete in child goroutine
	doneCh := make(chan struct{})
	go func() {
		resp, err = run.Get(actx)
		close(doneCh)
	}()

	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Second * 30
	}

	// heartbeat activity while waiting for workflow execution to complete
	for {
		select {
		// send heartbeats periodically
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx, run.ID())

		// return retryable error on worker close
		case <-activity.GetWorkerStopChannel(ctx):
			return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(context.Background(), time.Minute)
					defer cancel()
					if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, clientTaskQueueServiceOptions.convertError(err)
					}
				}
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		// handle workflow completion
		case <-doneCh:
			return resp, clientTaskQueueServiceOptions.convertError(err)
		}
	}
}

// FooWithBaz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Foo workflow with a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update via an activity
func (a *clientTaskQueueServiceActivities) FooWithBaz(ctx context.Context, input *xnsv1.UpdateWithStartRequest) (out *v1.BazOutput, err error) {
	// unmarshal workflow request
	var req v1.FooInput
	if err := input.GetInput().UnmarshalTo(&req); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling workflow request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.FooInput", input.GetInput().GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// unmarshal update request
	var update v1.BazInput
	if err := input.GetUpdate().UnmarshalTo(&update); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling update request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.BazInput", input.GetUpdate().GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// unmarshal workflow and update options
	swo := xns.UnmarshalStartWorkflowOptions(input.GetStartWorkflowOptions())
	uwo := xns.UnmarshalUpdateWorkflowOptions(input.GetUpdateWorkflowOptions())

	var run v1.FooRun
	var handle v1.BazHandle
	if activity.HasHeartbeatDetails(ctx) {
		// attach to existing update and execution
		var workflowID, runID, updateID string
		if err := activity.GetHeartbeatDetails(ctx, &workflowID, &runID, &updateID); err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(fmt.Errorf("error getting heartbeat details: %w", err))
		} else if workflowID == "" || runID == "" || updateID == "" {
			return nil, clientTaskQueueServiceOptions.convertError(fmt.Errorf("invalid heartbeat details: workflowID=%q runID=%q updateID=%s", workflowID, runID, updateID))
		}
		run = a.client.GetFoo(ctx, workflowID, runID)
		handle, err = a.client.GetBaz(ctx, client.GetWorkflowUpdateHandleOptions{
			RunID:      runID,
			UpdateID:   updateID,
			WorkflowID: workflowID,
		})
		if err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(fmt.Errorf("error getting update with id %s: %w", updateID, err))
		}
	} else {
		// execute update with start asynchronously
		handle, run, err = a.client.FooWithBazAsync(
			ctx,
			&req,
			&update,
			v1.NewFooWithBazOptions().WithFooOptions(
				v1.NewFooOptions().WithStartWorkflowOptions(swo),
			).WithBazOptions(
				v1.NewBazOptions().WithUpdateWorkflowOptions(uwo).WithWaitPolicy(client.WorkflowUpdateStageAccepted),
			),
		)
		if err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(fmt.Errorf("error executing update with start: %w", err))
		}
		activity.RecordHeartbeat(ctx, run.ID(), run.RunID(), handle.UpdateID())
	}

	// return early if detached
	if input.GetDetached() {
		return nil, nil
	}

	// initialize heartbeat interval duration
	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatTimeout := activity.GetInfo(ctx).HeartbeatTimeout
		if heartbeatTimeout > 0 {
			heartbeatInterval = heartbeatTimeout / 2
		} else {
			heartbeatInterval = time.Second * 30
		}
	}

	// wait for update to complete in child goroutine
	doneCh := make(chan struct{})
	go func() {
		defer close(doneCh)
		out, err = handle.Get(ctx)
	}()

	// heartbeat activity while waiting for update to complete
	for {
		select {
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx, run.ID(), run.RunID(), handle.UpdateID())

		case <-activity.GetWorkerStopChannel(ctx):
			return nil, clientTaskQueueServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopping"))

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopping"))
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(ctx, time.Minute)
					defer cancel()
					if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, clientTaskQueueServiceOptions.convertError(err)
					}
				}
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		case <-doneCh:
			return out, clientTaskQueueServiceOptions.convertError(err)
		}
	}
}

// GetQux retrieves a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow via an activity
func (a *clientTaskQueueServiceActivities) GetQux(ctx context.Context, input *xnsv1.GetWorkflowRequest) (out *v1.QuxOutput, err error) {
	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Second * 30
	}

	actx, cancel := context.WithCancel(context.Background())
	defer cancel()
	run := a.client.GetQux(actx, input.GetWorkflowId(), input.GetRunId())
	done := make(chan struct{})
	go func() {
		defer close(done)
		out, err = run.Get(actx)
	}()

	for {
		select {
		// send heartbeats periodically
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx)

		// return retryable error if the worker is stopping
		case <-activity.GetWorkerStopChannel(ctx):
			return nil, clientTaskQueueServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopped"))

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				activity.GetLogger(ctx).Info("worker is stopping")
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopped"))
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == enumsv1.PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(context.Background(), time.Minute)
					defer cancel()
					if parentClosePolicy == enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, clientTaskQueueServiceOptions.convertError(err)
					}
				}
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		// handle workflow completion
		case <-done:
			return out, clientTaskQueueServiceOptions.convertError(err)
		}
	}
}

// Qux executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow via an activity
func (a *clientTaskQueueServiceActivities) Qux(ctx context.Context, input *xnsv1.WorkflowRequest) (resp *v1.QuxOutput, err error) {
	// unmarshal workflow request
	var req v1.QuxInput
	if err := input.Request.UnmarshalTo(&req); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling workflow request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.QuxInput", input.Request.GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// initialize workflow execution
	actx := ctx
	if !input.GetDetached() {
		var cancel context.CancelFunc
		actx, cancel = context.WithCancel(context.Background())
		defer cancel()
	}
	var run v1.QuxRun
	run, err = a.client.QuxAsync(actx, &req, v1.NewQuxOptions().WithStartWorkflowOptions(
		xns.UnmarshalStartWorkflowOptions(input.GetStartWorkflowOptions()),
	))
	if err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(err)
	}

	// exit early if detached enabled
	if input.GetDetached() {
		return nil, nil
	}

	// otherwise, wait for execution to complete in child goroutine
	doneCh := make(chan struct{})
	go func() {
		resp, err = run.Get(actx)
		close(doneCh)
	}()

	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Second * 30
	}

	// heartbeat activity while waiting for workflow execution to complete
	for {
		select {
		// send heartbeats periodically
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx, run.ID())

		// return retryable error on worker close
		case <-activity.GetWorkerStopChannel(ctx):
			return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(context.Background(), time.Minute)
					defer cancel()
					if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, clientTaskQueueServiceOptions.convertError(err)
					}
				}
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		// handle workflow completion
		case <-doneCh:
			return resp, clientTaskQueueServiceOptions.convertError(err)
		}
	}
}

// QuxWithBar sends a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal to a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow via an activity
func (a *clientTaskQueueServiceActivities) QuxWithBar(ctx context.Context, input *xnsv1.WorkflowRequest) (resp *v1.QuxOutput, err error) {
	// unmarshal workflow request
	var req v1.QuxInput
	if err := input.Request.UnmarshalTo(&req); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling workflow request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.QuxInput", input.Request.GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// unmarshal signal request
	var signal v1.BarInput
	if err := input.Signal.UnmarshalTo(&signal); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling signal request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.BarInput", input.Signal.GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// initialize workflow execution
	actx := ctx
	if !input.GetDetached() {
		var cancel context.CancelFunc
		actx, cancel = context.WithCancel(context.Background())
		defer cancel()
	}
	var run v1.QuxRun
	run, err = a.client.QuxWithBarAsync(actx, &req, &signal, v1.NewQuxOptions().WithStartWorkflowOptions(
		xns.UnmarshalStartWorkflowOptions(input.GetStartWorkflowOptions()),
	))
	if err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(err)
	}

	// exit early if detached enabled
	if input.GetDetached() {
		return nil, nil
	}

	// otherwise, wait for execution to complete in child goroutine
	doneCh := make(chan struct{})
	go func() {
		resp, err = run.Get(actx)
		close(doneCh)
	}()

	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Second * 30
	}

	// heartbeat activity while waiting for workflow execution to complete
	for {
		select {
		// send heartbeats periodically
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx, run.ID())

		// return retryable error on worker close
		case <-activity.GetWorkerStopChannel(ctx):
			return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(context.Background(), time.Minute)
					defer cancel()
					if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, clientTaskQueueServiceOptions.convertError(err)
					}
				}
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		// handle workflow completion
		case <-doneCh:
			return resp, clientTaskQueueServiceOptions.convertError(err)
		}
	}
}

// QuxWithBaz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Qux workflow with a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update via an activity
func (a *clientTaskQueueServiceActivities) QuxWithBaz(ctx context.Context, input *xnsv1.UpdateWithStartRequest) (out *v1.BazOutput, err error) {
	// unmarshal workflow request
	var req v1.QuxInput
	if err := input.GetInput().UnmarshalTo(&req); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling workflow request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.QuxInput", input.GetInput().GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// unmarshal update request
	var update v1.BazInput
	if err := input.GetUpdate().UnmarshalTo(&update); err != nil {
		return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling update request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.BazInput", input.GetUpdate().GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// unmarshal workflow and update options
	swo := xns.UnmarshalStartWorkflowOptions(input.GetStartWorkflowOptions())
	uwo := xns.UnmarshalUpdateWorkflowOptions(input.GetUpdateWorkflowOptions())

	var run v1.QuxRun
	var handle v1.BazHandle
	if activity.HasHeartbeatDetails(ctx) {
		// attach to existing update and execution
		var workflowID, runID, updateID string
		if err := activity.GetHeartbeatDetails(ctx, &workflowID, &runID, &updateID); err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(fmt.Errorf("error getting heartbeat details: %w", err))
		} else if workflowID == "" || runID == "" || updateID == "" {
			return nil, clientTaskQueueServiceOptions.convertError(fmt.Errorf("invalid heartbeat details: workflowID=%q runID=%q updateID=%s", workflowID, runID, updateID))
		}
		run = a.client.GetQux(ctx, workflowID, runID)
		handle, err = a.client.GetBaz(ctx, client.GetWorkflowUpdateHandleOptions{
			RunID:      runID,
			UpdateID:   updateID,
			WorkflowID: workflowID,
		})
		if err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(fmt.Errorf("error getting update with id %s: %w", updateID, err))
		}
	} else {
		// execute update with start asynchronously
		handle, run, err = a.client.QuxWithBazAsync(
			ctx,
			&req,
			&update,
			v1.NewQuxWithBazOptions().WithQuxOptions(
				v1.NewQuxOptions().WithStartWorkflowOptions(swo),
			).WithBazOptions(
				v1.NewBazOptions().WithUpdateWorkflowOptions(uwo).WithWaitPolicy(client.WorkflowUpdateStageAccepted),
			),
		)
		if err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(fmt.Errorf("error executing update with start: %w", err))
		}
		activity.RecordHeartbeat(ctx, run.ID(), run.RunID(), handle.UpdateID())
	}

	// return early if detached
	if input.GetDetached() {
		return nil, nil
	}

	// initialize heartbeat interval duration
	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatTimeout := activity.GetInfo(ctx).HeartbeatTimeout
		if heartbeatTimeout > 0 {
			heartbeatInterval = heartbeatTimeout / 2
		} else {
			heartbeatInterval = time.Second * 30
		}
	}

	// wait for update to complete in child goroutine
	doneCh := make(chan struct{})
	go func() {
		defer close(doneCh)
		out, err = handle.Get(ctx)
	}()

	// heartbeat activity while waiting for update to complete
	for {
		select {
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx, run.ID(), run.RunID(), handle.UpdateID())

		case <-activity.GetWorkerStopChannel(ctx):
			return nil, clientTaskQueueServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopping"))

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopping"))
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(ctx, time.Minute)
					defer cancel()
					if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, clientTaskQueueServiceOptions.convertError(err)
					}
				}
				return nil, clientTaskQueueServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		case <-doneCh:
			return out, clientTaskQueueServiceOptions.convertError(err)
		}
	}
}

// Bar executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Bar signal via an activity
func (a *clientTaskQueueServiceActivities) Bar(ctx context.Context, input *xnsv1.SignalRequest) (err error) {
	// unmarshal signal request
	var req v1.BarInput
	if err := input.Request.UnmarshalTo(&req); err != nil {
		return clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling signal request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.BarInput", input.Request.GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}
	// execute signal in child goroutine
	doneCh := make(chan struct{})
	go func() {
		err = a.client.Bar(ctx, input.GetWorkflowId(), input.GetRunId(), &req)
		close(doneCh)
	}()

	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Second * 10
	}

	// heartbeat activity while waiting for signal to complete
	for {
		select {
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx)
		case <-ctx.Done():
			clientTaskQueueServiceOptions.convertError(ctx.Err())
		case <-doneCh:
			return clientTaskQueueServiceOptions.convertError(err)
		}
	}
}

// Baz executes a(n) test.client_task_queue.v1.ClientTaskQueueService.Baz update via an activity
func (a *clientTaskQueueServiceActivities) Baz(ctx context.Context, input *xnsv1.UpdateRequest) (resp *v1.BazOutput, err error) {
	var handle v1.BazHandle
	if activity.HasHeartbeatDetails(ctx) {
		// extract update id from heartbeat details
		var updateID string
		if err := activity.GetHeartbeatDetails(ctx, &updateID); err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(err)
		}

		// retrieve handle for existing update
		handle, err = a.client.GetBaz(ctx, client.GetWorkflowUpdateHandleOptions{
			WorkflowID: input.GetUpdateWorkflowOptions().GetWorkflowId(),
			RunID:      input.GetUpdateWorkflowOptions().GetRunId(),
			UpdateID:   updateID,
		})
		if err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(err)
		}
	} else {
		// unmarshal update request
		var req v1.BazInput
		if err := input.Request.UnmarshalTo(&req); err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
				fmt.Sprintf("error unmarshalling update request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/client-task-queue/v1.BazInput", input.Request.GetTypeUrl()),
				"InvalidArgument",
				err,
			))
		}

		uo := xns.UnmarshalUpdateWorkflowOptions(input.GetUpdateWorkflowOptions())
		uo.WaitForStage = client.WorkflowUpdateStageAccepted

		// initialize update execution
		handle, err = a.client.BazAsync(
			ctx,
			input.GetUpdateWorkflowOptions().GetWorkflowId(),
			input.GetUpdateWorkflowOptions().GetRunId(),
			&req,
			v1.NewBazOptions().WithUpdateWorkflowOptions(uo),
		)
		if err != nil {
			return nil, clientTaskQueueServiceOptions.convertError(err)
		}
		activity.RecordHeartbeat(ctx, handle.UpdateID())
	}

	// wait for update to complete in child goroutine
	doneCh := make(chan struct{})
	go func() {
		resp, err = handle.Get(ctx)
		close(doneCh)
	}()

	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Minute
	}

	// heartbeat activity while waiting for workflow update to complete
	for {
		select {
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx, handle.UpdateID())
		case <-ctx.Done():
			return nil, clientTaskQueueServiceOptions.convertError(ctx.Err())
		case <-doneCh:
			return resp, clientTaskQueueServiceOptions.convertError(err)
		}
	}
}
