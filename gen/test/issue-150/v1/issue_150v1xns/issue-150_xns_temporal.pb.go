// Code generated by protoc-gen-go_temporal. DO NOT EDIT.
// versions:
//
//	protoc-gen-go_temporal dev (latest)
//	protoc (unknown)
//
// source: test/issue-150/v1/issue-150.proto
package issue_150v1xns

import (
	"context"
	"errors"
	"fmt"
	temporalv1 "github.com/cludden/protoc-gen-go-temporal/gen/temporal/v1"
	xnsv1 "github.com/cludden/protoc-gen-go-temporal/gen/temporal/xns/v1"
	v1 "github.com/cludden/protoc-gen-go-temporal/gen/test/issue-150/v1"
	xns "github.com/cludden/protoc-gen-go-temporal/pkg/xns"
	uuid "github.com/google/uuid"
	enumsv1 "go.temporal.io/api/enums/v1"
	activity "go.temporal.io/sdk/activity"
	client "go.temporal.io/sdk/client"
	temporal "go.temporal.io/sdk/temporal"
	worker "go.temporal.io/sdk/worker"
	workflow "go.temporal.io/sdk/workflow"
	anypb "google.golang.org/protobuf/types/known/anypb"
	durationpb "google.golang.org/protobuf/types/known/durationpb"
	"time"
)

// Issue150ServiceOptions is used to configure issue_150.v1.Issue150Service xns activity registration
type Issue150ServiceOptions struct {
	// errorConverter is used to customize error
	errorConverter func(error) error
	// filter is used to filter xns activity registrations. It receives as
	// input the original activity name, and should return one of the following:
	// 1. the original activity name, for no changes
	// 2. a modified activity name, to override the original activity name
	// 3. an empty string, to skip registration
	filter func(string) string
}

// NewIssue150ServiceOptions initializes a new Issue150ServiceOptions value
func NewIssue150ServiceOptions() *Issue150ServiceOptions {
	return &Issue150ServiceOptions{}
}

// WithErrorConverter overrides the default error converter applied to xns activity errors
func (opts *Issue150ServiceOptions) WithErrorConverter(errorConverter func(error) error) *Issue150ServiceOptions {
	opts.errorConverter = errorConverter
	return opts
}

// Filter is used to filter registered xns activities or customize their name
func (opts *Issue150ServiceOptions) WithFilter(filter func(string) string) *Issue150ServiceOptions {
	opts.filter = filter
	return opts
}

// convertError is applied to all xns activity errors
func (opts *Issue150ServiceOptions) convertError(err error) error {
	if err == nil {
		return nil
	}
	if opts != nil && opts.errorConverter != nil {
		return opts.errorConverter(err)
	}
	return xns.ErrorToApplicationError(err)
}

// filterActivity is used to filter xns activity registrations
func (opts *Issue150ServiceOptions) filterActivity(name string) string {
	if opts == nil || opts.filter == nil {
		return name
	}
	return opts.filter(name)
}

// issue150ServiceOptions is a reference to the Issue150ServiceOptions initialized at registration
var issue150ServiceOptions *Issue150ServiceOptions

// RegisterIssue150ServiceActivities registers issue_150.v1.Issue150Service cross-namespace activities
func RegisterIssue150ServiceActivities(r worker.ActivityRegistry, c v1.Issue150ServiceClient, options ...*Issue150ServiceOptions) {
	if issue150ServiceOptions == nil && len(options) > 0 && options[0] != nil {
		issue150ServiceOptions = options[0]
	}
	a := &issue150ServiceActivities{c}
	if name := issue150ServiceOptions.filterActivity("issue_150.v1.Issue150Service.CancelWorkflow"); name != "" {
		r.RegisterActivityWithOptions(a.CancelWorkflow, activity.RegisterOptions{Name: name})
	}
	if name := issue150ServiceOptions.filterActivity(v1.ExplicitPriorityWorkflowName); name != "" {
		r.RegisterActivityWithOptions(a.ExplicitPriority, activity.RegisterOptions{Name: name})
	}
	if name := issue150ServiceOptions.filterActivity("issue_150.v1.Issue150Service.GetExplicitPriority"); name != "" {
		r.RegisterActivityWithOptions(a.GetExplicitPriority, activity.RegisterOptions{Name: name})
	}
}

// ExplicitPriorityWorkflowOptions are used to configure a(n) issue_150.v1.Issue150Service.ExplicitPriority workflow execution
type ExplicitPriorityWorkflowOptions struct {
	ActivityOptions      *workflow.ActivityOptions
	Detached             bool
	HeartbeatInterval    time.Duration
	HeartbeatTimeout     time.Duration
	ParentClosePolicy    enumsv1.ParentClosePolicy
	StartWorkflowOptions *client.StartWorkflowOptions
}

// NewExplicitPriorityWorkflowOptions initializes a new ExplicitPriorityWorkflowOptions value
func NewExplicitPriorityWorkflowOptions() *ExplicitPriorityWorkflowOptions {
	return &ExplicitPriorityWorkflowOptions{}
}

// Build initializes the activity context and input
func (opts *ExplicitPriorityWorkflowOptions) Build(ctx workflow.Context, input *v1.Input) (workflow.Context, *xnsv1.WorkflowRequest, error) {
	// initialize start workflow options
	swo := client.StartWorkflowOptions{}
	if opts.StartWorkflowOptions != nil {
		swo = *opts.StartWorkflowOptions
	}

	// initialize workflow id if not set
	if swo.ID == "" {
		if err := workflow.SideEffect(ctx, func(ctx workflow.Context) any {
			id, err := uuid.NewRandom()
			if err != nil {
				workflow.GetLogger(ctx).Error("error generating workflow id", "error", err)
				return nil
			}
			return id
		}).Get(&swo.ID); err != nil {
			return nil, nil, err
		}
	}
	if swo.ID == "" {
		return nil, nil, temporal.NewNonRetryableApplicationError("workflow id is required", "InvalidArgument", nil)
	}

	// marshal workflow request protobuf message
	inputpb, err := anypb.New(input)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling workflow request: %w", err)
	}

	// marshal start workflow options protobuf message
	swopb, err := xns.MarshalStartWorkflowOptions(swo)
	if err != nil {
		return ctx, nil, fmt.Errorf("error marshalling start workflow options: %w", err)
	}

	// marshal parent close policy protobuf message
	var parentClosePolicy temporalv1.ParentClosePolicy
	switch opts.ParentClosePolicy {
	case enumsv1.PARENT_CLOSE_POLICY_ABANDON:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_ABANDON
	case enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL
	case enumsv1.PARENT_CLOSE_POLICY_TERMINATE:
		parentClosePolicy = temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE
	}

	// initialize xns activity options
	ao := workflow.ActivityOptions{}
	if opts.ActivityOptions != nil {
		ao = *opts.ActivityOptions
	}

	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = time.Second * 60
	}

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = time.Hour * 24
	}

	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	// configure heartbeat interval
	if opts.HeartbeatInterval == 0 {
		opts.HeartbeatInterval = ao.HeartbeatTimeout / 2
	}

	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.WorkflowRequest{
		Detached:             opts.Detached,
		HeartbeatInterval:    durationpb.New(opts.HeartbeatInterval),
		ParentClosePolicy:    parentClosePolicy,
		Request:              inputpb,
		StartWorkflowOptions: swopb,
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (opts *ExplicitPriorityWorkflowOptions) WithActivityOptions(ao workflow.ActivityOptions) *ExplicitPriorityWorkflowOptions {
	opts.ActivityOptions = &ao
	return opts
}

// WithDetached can be used to start a workflow execution and exit immediately
func (opts *ExplicitPriorityWorkflowOptions) WithDetached(d bool) *ExplicitPriorityWorkflowOptions {
	opts.Detached = d
	return opts
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (opts *ExplicitPriorityWorkflowOptions) WithHeartbeatInterval(d time.Duration) *ExplicitPriorityWorkflowOptions {
	opts.HeartbeatInterval = d
	return opts
}

// WithHeartbeatTimeout can be used to customize the activity heartbeat timeout
func (opts *ExplicitPriorityWorkflowOptions) WithHeartbeatTimeout(d time.Duration) *ExplicitPriorityWorkflowOptions {
	opts.HeartbeatTimeout = d
	return opts
}

// WithParentClosePolicy can be used to customize the cancellation propagation behavior
func (opts *ExplicitPriorityWorkflowOptions) WithParentClosePolicy(policy enumsv1.ParentClosePolicy) *ExplicitPriorityWorkflowOptions {
	opts.ParentClosePolicy = policy
	return opts
}

// WithStartWorkflowOptions can be used to customize the start workflow options
func (opts *ExplicitPriorityWorkflowOptions) WithStartWorkflow(swo client.StartWorkflowOptions) *ExplicitPriorityWorkflowOptions {
	opts.StartWorkflowOptions = &swo
	return opts
}

// ExplicitPriorityRun provides a handle to a issue_150.v1.Issue150Service.ExplicitPriority workflow execution
type ExplicitPriorityRun interface {
	// Cancel cancels the workflow
	Cancel(workflow.Context) error

	// Future returns the inner workflow.Future
	Future() workflow.Future

	// Get returns the inner workflow.Future
	Get(workflow.Context) (*v1.Output, error)

	// ID returns the workflow id
	ID() string
}

// explicitPriorityRun provides a(n) ExplicitPriorityRun implementation
type explicitPriorityRun struct {
	cancel            func()
	ctx               workflow.Context
	future            workflow.Future
	id                string
	heartbeatInterval time.Duration
	parentClosePolicy enumsv1.ParentClosePolicy
}

// Cancel the underlying workflow execution
func (r *explicitPriorityRun) Cancel(ctx workflow.Context) error {
	if r.cancel != nil {
		r.cancel()
		if _, err := r.Get(ctx); err != nil && !errors.Is(err, workflow.ErrCanceled) {
			return err
		}
		return nil
	}
	return CancelIssue150ServiceWorkflow(ctx, r.id, "")
}

// Future returns the underlying activity future
func (r *explicitPriorityRun) Future() workflow.Future {
	if r.future == nil {
		rr := GetExplicitPriorityAsync(r.ctx, r.id, "").(*explicitPriorityRun)
		r.future = rr.future
		r.cancel = rr.cancel
	}
	return r.future
}

// Get blocks on activity completion and returns the underlying workflow result
func (r *explicitPriorityRun) Get(ctx workflow.Context) (*v1.Output, error) {
	ctx, cancel := workflow.WithCancel(ctx)
	if r.future == nil {
		rr := GetExplicitPriorityAsync(ctx, r.id, "", NewGetExplicitPriorityOptions().WithParentClosePolicy(r.parentClosePolicy).WithHeartbeatInterval(r.heartbeatInterval)).(*explicitPriorityRun)
		r.future = rr.future
		r.cancel = cancel
	}
	var resp v1.Output
	if err := r.future.Get(ctx, &resp); err != nil {
		return nil, err
	}
	return &resp, nil
}

// ID returns the underlying workflow id
func (r *explicitPriorityRun) ID() string {
	return r.id
}

// ExplicitPriority executes a(n) issue_150.v1.Issue150Service.ExplicitPriority workflow and blocks until error or response is received
func ExplicitPriority(ctx workflow.Context, req *v1.Input, opts ...*ExplicitPriorityWorkflowOptions) (*v1.Output, error) {
	run, err := ExplicitPriorityAsync(ctx, req, opts...)
	if err != nil {
		return nil, err
	}
	return run.Get(ctx)
}

// ExplicitPriorityAsync executes a(n) issue_150.v1.Issue150Service.ExplicitPriority workflow and returns a handle to the underlying activity
func ExplicitPriorityAsync(ctx workflow.Context, input *v1.Input, opts ...*ExplicitPriorityWorkflowOptions) (ExplicitPriorityRun, error) {
	activityName := issue150ServiceOptions.filterActivity(v1.ExplicitPriorityWorkflowName)
	if activityName == "" {
		return nil, temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("no activity registered for %s", v1.ExplicitPriorityWorkflowName),
			"Unimplemented",
			nil,
		)
	}

	var opt *ExplicitPriorityWorkflowOptions
	if len(opts) > 0 && opts[0] != nil {
		opt = opts[0]
	} else {
		opt = NewExplicitPriorityWorkflowOptions()
	}
	ctx, req, err := opt.Build(ctx, input)
	if err != nil {
		return nil, issue150ServiceOptions.convertError(err)
	}
	ctx, cancel := workflow.WithCancel(ctx)
	return &explicitPriorityRun{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     req.GetStartWorkflowOptions().GetId(),
	}, nil
}

// GetExplicitPriority returns a(n) issue_150.v1.Issue150Service.ExplicitPriority workflow execution
func GetExplicitPriority(ctx workflow.Context, workflowID string, runID string, options ...*GetExplicitPriorityOptions) (out *v1.Output, err error) {
	out, err = GetExplicitPriorityAsync(ctx, workflowID, runID, options...).Get(ctx)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// GetExplicitPriorityAsync returns a handle to a(n) issue_150.v1.Issue150Service.ExplicitPriority workflow execution
func GetExplicitPriorityAsync(ctx workflow.Context, workflowID string, runID string, options ...*GetExplicitPriorityOptions) ExplicitPriorityRun {
	activityName := issue150ServiceOptions.filterActivity("issue_150.v1.Issue150Service.GetExplicitPriority")
	if activityName == "" {
		f, set := workflow.NewFuture(ctx)
		set.SetError(temporal.NewNonRetryableApplicationError(fmt.Sprintf("no activity registered for %s", activityName), "Unimplemented", nil))
		return &explicitPriorityRun{
			future: f,
			id:     workflowID,
		}
	}
	var opt *GetExplicitPriorityOptions
	if len(options) > 0 && options[0] != nil {
		opt = options[0]
	} else {
		opt = NewGetExplicitPriorityOptions()
	}
	ctx, req, err := opt.Build(ctx, workflowID, runID)
	if err != nil {
		f, set := workflow.NewFuture(ctx)
		set.SetError(issue150ServiceOptions.convertError(temporal.NewNonRetryableApplicationError(fmt.Sprintf("no activity registered for %s", activityName), "Unimplemented", nil)))
		return &explicitPriorityRun{
			future: f,
			id:     workflowID,
		}
	}
	ctx, cancel := workflow.WithCancel(ctx)
	return &explicitPriorityRun{
		cancel: cancel,
		future: workflow.ExecuteActivity(ctx, activityName, req),
		id:     workflowID,
	}
}

// GetExplicitPriorityOptions are used to configure a(n) issue_150.v1.Issue150Service.ExplicitPriority workflow execution getter activity
type GetExplicitPriorityOptions struct {
	activityOptions   *workflow.ActivityOptions
	heartbeatInterval time.Duration
	parentClosePolicy enumsv1.ParentClosePolicy
}

// NewGetExplicitPriorityOptions initializes a new GetExplicitPriorityOptions value
func NewGetExplicitPriorityOptions() *GetExplicitPriorityOptions {
	return &GetExplicitPriorityOptions{}
}

// Build initializes the activity context and input
func (opt *GetExplicitPriorityOptions) Build(ctx workflow.Context, workflowID string, runID string) (workflow.Context, *xnsv1.GetWorkflowRequest, error) {
	if opt.heartbeatInterval == 0 {
		opt.heartbeatInterval = 30000000000 // 30 seconds
	}

	// configure activity options
	var ao workflow.ActivityOptions
	if opt.activityOptions != nil {
		ao = *opt.activityOptions
	} else {
		ao = workflow.ActivityOptions{}
	}
	if ao.HeartbeatTimeout == 0 {
		ao.HeartbeatTimeout = 60000000000 // 1 minute
	}
	// WaitForCancellation must be set otherwise the underlying workflow is not guaranteed to be canceled
	ao.WaitForCancellation = true

	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.ScheduleToCloseTimeout = 86400000000000 // 1 day
	}
	ctx = workflow.WithActivityOptions(ctx, ao)

	return ctx, &xnsv1.GetWorkflowRequest{
		HeartbeatInterval: durationpb.New(opt.heartbeatInterval),
		ParentClosePolicy: opt.parentClosePolicy,
		RunId:             runID,
		WorkflowId:        workflowID,
	}, nil
}

// WithActivityOptions can be used to customize the activity options
func (o *GetExplicitPriorityOptions) WithActivityOptions(ao workflow.ActivityOptions) *GetExplicitPriorityOptions {
	o.activityOptions = &ao
	return o
}

// WithHeartbeatInterval can be used to customize the activity heartbeat interval
func (o *GetExplicitPriorityOptions) WithHeartbeatInterval(d time.Duration) *GetExplicitPriorityOptions {
	o.heartbeatInterval = d
	return o
}

// WithParentClosePolicy can be used to customize the cancellation propagation behavior
func (o *GetExplicitPriorityOptions) WithParentClosePolicy(policy enumsv1.ParentClosePolicy) *GetExplicitPriorityOptions {
	o.parentClosePolicy = policy
	return o
}

// CancelIssue150ServiceWorkflow cancels an existing workflow
func CancelIssue150ServiceWorkflow(ctx workflow.Context, workflowID string, runID string) error {
	return CancelIssue150ServiceWorkflowAsync(ctx, workflowID, runID).Get(ctx, nil)
}

// CancelIssue150ServiceWorkflowAsync cancels an existing workflow
func CancelIssue150ServiceWorkflowAsync(ctx workflow.Context, workflowID string, runID string) workflow.Future {
	activityName := issue150ServiceOptions.filterActivity("issue_150.v1.Issue150Service.CancelWorkflow")
	if activityName == "" {
		f, s := workflow.NewFuture(ctx)
		s.SetError(temporal.NewNonRetryableApplicationError(
			"no activity registered for issue_150.v1.Issue150Service.CancelWorkflow",
			"Unimplemented",
			nil,
		))
		return f
	}
	ao := workflow.GetActivityOptions(ctx)
	if ao.StartToCloseTimeout == 0 && ao.ScheduleToCloseTimeout == 0 {
		ao.StartToCloseTimeout = time.Minute
	}
	ctx = workflow.WithActivityOptions(ctx, ao)
	return workflow.ExecuteActivity(ctx, activityName, workflowID, runID)
}

// issue150ServiceActivities provides activities that can be used to interact with a(n) Issue150Service service's workflow, queries, signals, and updates across namespaces
type issue150ServiceActivities struct {
	client v1.Issue150ServiceClient
}

// CancelWorkflow cancels an existing workflow execution
func (a *issue150ServiceActivities) CancelWorkflow(ctx context.Context, workflowID string, runID string) error {
	return a.client.CancelWorkflow(ctx, workflowID, runID)
}

// GetExplicitPriority retrieves a(n) issue_150.v1.Issue150Service.ExplicitPriority workflow via an activity
func (a *issue150ServiceActivities) GetExplicitPriority(ctx context.Context, input *xnsv1.GetWorkflowRequest) (out *v1.Output, err error) {
	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Second * 30
	}

	actx, cancel := context.WithCancel(context.Background())
	defer cancel()
	run := a.client.GetExplicitPriority(actx, input.GetWorkflowId(), input.GetRunId())
	done := make(chan struct{})
	go func() {
		defer close(done)
		out, err = run.Get(actx)
	}()

	for {
		select {
		// send heartbeats periodically
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx)

		// return retryable error if the worker is stopping
		case <-activity.GetWorkerStopChannel(ctx):
			return nil, issue150ServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopped"))

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				activity.GetLogger(ctx).Info("worker is stopping")
				return nil, issue150ServiceOptions.convertError(temporal.NewApplicationError("worker is stopping", "WorkerStopped"))
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == enumsv1.PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(context.Background(), time.Minute)
					defer cancel()
					if parentClosePolicy == enumsv1.PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, issue150ServiceOptions.convertError(err)
					}
				}
				return nil, issue150ServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		// handle workflow completion
		case <-done:
			return out, issue150ServiceOptions.convertError(err)
		}
	}
}

// ExplicitPriority executes a(n) issue_150.v1.Issue150Service.ExplicitPriority workflow via an activity
func (a *issue150ServiceActivities) ExplicitPriority(ctx context.Context, input *xnsv1.WorkflowRequest) (resp *v1.Output, err error) {
	// unmarshal workflow request
	var req v1.Input
	if err := input.Request.UnmarshalTo(&req); err != nil {
		return nil, issue150ServiceOptions.convertError(temporal.NewNonRetryableApplicationError(
			fmt.Sprintf("error unmarshalling workflow request of type %s as github.com/cludden/protoc-gen-go-temporal/gen/test/issue-150/v1.Input", input.Request.GetTypeUrl()),
			"InvalidArgument",
			err,
		))
	}

	// initialize workflow execution
	actx := ctx
	if !input.GetDetached() {
		var cancel context.CancelFunc
		actx, cancel = context.WithCancel(context.Background())
		defer cancel()
	}
	var run v1.ExplicitPriorityRun
	run, err = a.client.ExplicitPriorityAsync(actx, &req, v1.NewExplicitPriorityOptions().WithStartWorkflowOptions(
		xns.UnmarshalStartWorkflowOptions(input.GetStartWorkflowOptions()),
	))
	if err != nil {
		return nil, issue150ServiceOptions.convertError(err)
	}

	// exit early if detached enabled
	if input.GetDetached() {
		return nil, nil
	}

	// otherwise, wait for execution to complete in child goroutine
	doneCh := make(chan struct{})
	go func() {
		resp, err = run.Get(actx)
		close(doneCh)
	}()

	heartbeatInterval := input.GetHeartbeatInterval().AsDuration()
	if heartbeatInterval == 0 {
		heartbeatInterval = time.Second * 30
	}

	// heartbeat activity while waiting for workflow execution to complete
	for {
		select {
		// send heartbeats periodically
		case <-time.After(heartbeatInterval):
			activity.RecordHeartbeat(ctx, run.ID())

		// return retryable error on worker close
		case <-activity.GetWorkerStopChannel(ctx):
			return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")

		// catch parent activity context cancellation. in most cases, this should indicate a
		// server-sent cancellation, but there's a non-zero possibility that this cancellation
		// is received due to the worker stopping, prior to detecting the closing of the worker
		// stop channel. to give us an opportunity to detect a cancellation stemming from the
		// worker closing, we again check to see if the worker stop channel is closed before
		// propagating the cancellation
		case <-ctx.Done():
			select {
			case <-activity.GetWorkerStopChannel(ctx):
				return nil, temporal.NewApplicationError("worker is stopping", "WorkerStopped")
			default:
				parentClosePolicy := input.GetParentClosePolicy()
				if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL || parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_TERMINATE {
					disconnectedCtx, cancel := context.WithTimeout(context.Background(), time.Minute)
					defer cancel()
					if parentClosePolicy == temporalv1.ParentClosePolicy_PARENT_CLOSE_POLICY_REQUEST_CANCEL {
						err = run.Cancel(disconnectedCtx)
					} else {
						err = run.Terminate(disconnectedCtx, "xns activity cancellation received", "error", ctx.Err())
					}
					if err != nil {
						return nil, issue150ServiceOptions.convertError(err)
					}
				}
				return nil, issue150ServiceOptions.convertError(temporal.NewCanceledError(ctx.Err().Error()))
			}

		// handle workflow completion
		case <-doneCh:
			return resp, issue150ServiceOptions.convertError(err)
		}
	}
}
